# 판결극장 (Verdict Theater) — 프로젝트 지침서 v3 (최종)

> 이 파일이 판결극장의 모든 결정사항이다. Claude Code가 시작 시 자동으로 읽는다.
>
> **저장소**: `jtaechul/verdict-theater` (**비공개**). 이 파일은 저장소 루트에 있다.
> 아래 "2. 시스템 구성"의 `/prompts`, `/state` 등은 전부 저장소 루트 기준 경로다.
> 비공개로 둔 이유는 0번에 있다 — 대본 생성 프롬프트가 이 사업의 유일한 진입장벽이기 때문이다.

---

## 0. 이 프로젝트에서 가장 중요한 한 가지

**대본 생성 프롬프트가 이 사업의 유일한 핵심 자산이다.**

판례는 사건 골격과 결말만 제공한다. 12분 대본의 **약 82%는 창작**이다. 판례 API는 누구나 열 수 있으므로 진입장벽은 데이터가 아니라 프롬프트다.

`/prompts` 폴더를 최우선으로 관리하고 버전을 남긴다.

---

## 0-2. ⭐⭐⭐ 루미나 프롬프트 3대 금지 (어기면 영상이 아예 안 만들어진다)

> 셋 다 **실제로 겪고** 만든 규칙이다. 하나라도 어기면 크레딧만 쓰고 실패한다.
> 코드가 자동으로 막는다 (`src/series.py` · 자체 점검이 푸시마다 검사).

### ① 옷·얼굴·머리 모양을 **한 글자도** 적지 않는다

운영자가 루미나에서 **main body reference** 로 등장인물 **전신 참조 이미지**를
넣는다. 루미나는 그 사람이 무엇을 입었는지 **이미 알고 있다.** 글로 또 적으면
두 지시가 싸우고, 컷마다 이긴 쪽이 달라져 **옷이 계속 바뀐다.**

- `SUBJECT` 줄은 **아예 없다** (규격에서 뺐다)
- 누가 화면에 있는지는 `SHOT` 줄에 **이름만** 적는다
- 옷을 만지는 동작도 안 된다 (`grips her own sleeve` ✗)
- 검사: `wear_bait()` — 어느 줄에 있어도 잡는다. 가구(`a coat hook`)는 봐준다

### ② **"하지 마" 로 적지 않는다. 바라는 것만 적는다**

**안전 검사기는 부정을 이해하지 못하고 낱말만 본다.**
우리가 `Avoid: the person changing clothes or face mid-shot, swapping in a
different person` 이라고 **막으려고** 적은 것을, 검사기는
"옷 벗기 · 얼굴 바꾸기(딥페이크)를 **해 달라**" 로 읽었다.
→ 영상 만들기 실패 (`code=23007`).

- 규격의 마지막 줄 이름을 `Avoid:` → **`KEEP:`** 으로 바꿨다.
  이름이 negative 면 다음 사람이 또 부정문을 채워 넣는다.
- 검사: `negative_words()` — 부정문이 있으면 알려 준다

### ③ 안전 검사에 걸리는 **낱말**을 쓰지 않는다

뜻이 멀쩡해도 **글자 자체가** 걸린다.

| 걸린 낱말 | 왜 | 대신 |
|---|---|---|
| `adult` | 성인물 신호어 | `true-to-life` |
| `restrained` | 결박으로 읽힌다 | `calm` |
| `swap` / `swapping` | 얼굴 바꾸기(딥페이크) | 쓰지 않는다 |
| `changing clothes` | 옷 벗기 | 쓰지 않는다 |

- 검사: `risky_words()` — 있으면 **대본을 반려한다**
- 낱말이 하나 더 발견되면 `src/series.py` 의 `RISKY` 에 **추가하고 까닭을 적는다**

> ⚠️ 이 규칙은 **컷 프롬프트뿐 아니라 인물 기준 사진 프롬프트(`charsheet.py`)
> 에도 똑같이 적용된다.** 실제로 거기에도 `adult` · `restrained` 가 박혀 있었다.

> ⚠️ 이 규칙은 **컷 프롬프트뿐 아니라 인물 기준 사진 프롬프트(`charsheet.py`)
> 에도 똑같이 적용된다.** 실제로 거기에도 `adult` · `restrained` 가 박혀 있었다.

---

## 0-3. ⭐ 화면 문법 (2026-08-25 확정)

| 자리 | 무엇 | 언제 |
|---|---|---|
| 위 검은 칸 | **후킹** | 첫 컷부터 **마지막 컷 끝까지** 계속 |
| 자막 칸 오른쪽 위 | **때** (2013년 8월) | 각 화 첫 컷 앞 3초 |
| 화면 가운데 | **다음 화 예고** | 마지막 컷에서 **말이 끝난 뒤** 2초 |

### 다음 화 예고 — **영상 칸만** 어둡게 한다

TV 드라마처럼 반투명 검정 위에 예고를 띄우되, **화면 전체를 덮지 않는다.**
위아래 띠는 원래 검은색이므로 영상 칸만 덮으면 화면 전체가 고르게 어두워
보이면서 **후킹은 위에서 그대로 밝다.**

- 큰 줄은 다음 화의 **제목이 아니라 후킹** (제목은 목차처럼 밋밋하다)
- **길이를 1초도 안 늘린다.** 쇼츠는 뒤에 붙이면 평균 시청률이 깎인다
  (30초 미만은 65%가 확산 기준)
- 마지막 화는 「완 결」로 자동 전환

### 샷 — 두 사람을 늘 같이 보여 주지 않는다

세로 드라마 작법: *"대사는 한 명씩 번갈아 잡고, 주고받는 느낌은 컷 전환이
지게 하라. 가로가 두 사람을 좌우로 놓는다면 세로는 앞뒤로 쌓는다."*

- **말하는 사람이 한 명뿐인 컷은 무조건 클로즈업** — 얼굴이 3배 커진다
  (4:3으로 잘라 쓰므로 얼굴 크기가 특히 중요하다)
- 샷 순서는 화마다 다르게 (`shots` 로 정한다). 16화가 같은 순서면 단조롭다
- 한 화 안에 **세 가지 크기**를 다 쓴다
- 입 모양·소리 지시는 **말한 사람 수**를 따라간다
  (혼자 말하는 컷에 "둘 다 입을 움직여라" 가 붙으면 안 된다)

---

## 0-1. ⭐⭐⭐ 짝 목록 — 한쪽만 고치면 안 되는 것들

> 손님: **"이런 코드 실수는 왜 자꾸 범하는거야?"**
>
> 세어 보니 실수의 대부분이 **한 모양**이었다 —
> **정하는 곳과 쓰는 곳이 둘인데 한쪽만 고친다.**

| 한쪽 | 짝 | 안 맞추면 |
|---|---|---|
| `series.HEAD_FIX` (프롬프트 첫 줄) | `series.STYLE_FIX` (컷 화풍) | 첫 줄이 가장 세게 먹어 영상이 앞뒤로 갈린다 |
| `series.STYLE_FIX` (컷 화풍) | `charsheet.LOOK` (인물 화풍) | 컷과 인물이 따로 놀아 얼굴이 안 잡힌다 |
| 고정 줄을 고침 | `prompts/series_gen.md` 예시도 같이 | 새로 짓는 대본만 옛 화풍으로 나온다 |
| 화풍이 **실사** | `charsheet.AVOID` 가 그림체를 막아야 | 그림이 섞인다 (**2번 났다**) |
| 화풍이 **그림체** | `charsheet.PHOTO_WORDS` 가 사진 말을 떼어내야 | 사진풍이 섞인다 |
| `WORKFLOWS` 에 단추를 적음 | `wfList([...])` 에 넣어야 | 단추가 화면에 안 나온다 |
| 칸 기본값을 `v:` 로 적음 | 읽는 코드도 `v` 를 봐야 | 칸이 늘 비어 있다 |
| `rewrite_story` 가 만드는 고정 줄 | `series.py` 의 상수를 써야 | 검사기가 모르는 문장이 나간다 |
| 이야기 파일에 설정을 적음 | `rewrite_story` 가 읽어야 | 적어만 두고 조용히 무시된다 |
| 인물을 늘림 | `ORDER` · `VOICES` · `WHO_KO` 이름표 | 자막 위에 아무것도 안 그려진다 |
| 입 모양·소리는 **말한 사람** 기준 | 샷 크기도 말한 사람 기준 | 혼자 말하는 컷에 어깨너머 샷이 붙는다 |
| 지시문·재료를 고침 | **만들어 둔 것을 다시 쓰는 자리**도 지문을 봐야 | 고쳐도 옛것이 그대로 나온다 (화풍 사고) |

검사: `python3 tools/pair_check.py` (0원, 매 푸시마다 자동) ·
단추·기본값 짝은 `node tools/check_admin.mjs` ·
다시 쓰기는 `python3 tools/reuse_test.py` (실제로 돌려서 센다)

> ⚠️ **새 짝을 만들 때 여기에 적는다.** 사고가 난 뒤에 붙이지 말 것.
> 사고 뒤에 붙이는 검사는 **같은 사고만** 막는다.

---

## 0-3-2. ⭐⭐⭐ 화풍 — 실사 (2026-08-26 확정)

> 운영자: **"실사로 가자."** 5070 시청자에게는 그림체보다 실사가
> **'실제 있었던 일'** 로 읽힌다.

- 컷 화풍(`series.STYLE_FIX`)과 인물 기준 그림(`charsheet.LOOK`)은 **늘 같이** 바꾼다.
  따로 놀면 얼굴이 안 잡힌다
- **화풍을 바꿀 때는 반대편('하지 마라') 목록도 같이 뒤집는다.**
  실사로 온 지금은 `cartoon · anime · illustration · drawing` 을 막아야 한다
  (이걸 안 해서 두 번 걸렸다)
- **정책에 걸리는 말은 안 쓴다** — `actor · celebrity · live-action · real person`
  이 들어가면 "유명인 동영상 생성" 으로 통째로 반려된다
- 얼굴은 `FACE AND HAIR`, 몸집·자세는 `BUILD`, 화풍만 `LOOK` 이 진다.
  공용 `LOOK` 이 얼굴까지 정하면 **사람마다 다른 인상을 줄 수가 없다**
- **화풍을 바꿀 때 고쳐야 하는 자리는 넷이다** — `series.HEAD_FIX`(프롬프트 첫 줄) ·
  `series.STYLE_FIX` · `charsheet.LOOK` · `charsheet.AVOID`. 그리고 대본 짓는
  프롬프트 `prompts/series_gen.md` 의 예시까지. **한 자리라도 빠지면 화풍이 섞인다**
  (실제로 머리말만 `illustrated drama` 로 남아 영상이 앞뒤로 갈렸다)
- 다섯 사람의 기준 그림은 전부 `src/charsheet.py` 가 짓는다.
  손으로 적어 두면 화풍을 바꿔도 그 사람만 옛 화풍으로 남는다

### 인물 인상

| 인물 | 얼굴·머리 | 몸집 |
|---|---|---|
| 아내(52) | 갸름한 얼굴, 잔주름 있는 지친 눈, 낮게 묶은 짙은 갈색 머리 | 평범 |
| 남편(55) | 각진 얼굴, 깊은 이마 주름, 짧게 넘긴 검은 머리 | 평범 |
| **내연녀(38)** | **또렷한 V라인, 진한 아이라인·긴 속눈썹, 정돈된 눈썹, 붉은 립, 윤기 나는 긴 웨이브 갈색 머리** | **큰 키에 늘씬한 몸매·긴 다리, 곧은 자세, 흐트러짐 없이 단정** |
| 딸(22) | 둥근 얼굴, 낮게 묶은 검은 생머리 | 평범 |
| 변호사(45) | 긴 얼굴, 짧게 빗어 넘긴 검은 머리 | 평범 |

### 내연녀 옷차림 (2026-08-27 운영자 지시 — "화장도 치마도 입고 옷도 눈에 띄게")

와인색 무릎길이 타이트 펜슬 스커트(옆트임) · 몸에 붙는 크림색 실크 블라우스(목단추 풀림) ·
살구빛 스타킹 대신 얇은 검정 스타킹 · 광택 있는 검정 하이힐 · 가는 금목걸이와 작은 금귀걸이.

- **옷은 인물 카드(`CHAR_LOOKS` → `WEARING:`)에만 적는다.** 컷 프롬프트에 옷을 적으면
  규격 검사가 막는다(`wear_bait`) — 컷마다 옷이 달라지는 사고를 막으려고 둔 규칙
- 옛 세 사람(본처·남편·내연녀)의 생김새는 `data/series/S001_story.py` 의 `CHAR_LOOKS`
  에서 정한다. JSON 만 고치면 대본을 다시 지을 때 조용히 되돌아간다

⚠️ 매혹적인 인상은 `sexy · seductive · erotic` 이 아니라
**`striking · elegant · graceful · poised`** 로 적는다. 앞의 말들은 안전 검사에
걸려 **영상이 아예 안 만들어진다.**

---

## 0-3-3. ⭐⭐⭐ 만들어 둔 것을 다시 쓰는 규칙 (2026-08-27 확정)

> 손님: **"그림체는 실사로 가기로 했는데 영상 끝부분에는 일부러 애니메이션풍으로 바꾼거야?"**
> 아니다. **옛 인물 카드가 그대로 다시 쓰인 것**이다.

돈이 나가는 것(인물 카드·컷 그림·컷 영상)은 만들어 두고 다음 실행에서 다시 쓴다.
그 판단이 **"파일이 있으면 건너뛴다"** 였다 — 무엇으로 만든 것인지를 안 보니
지시문을 고쳐도 옛것이 나왔다. 실제로 이렇게 됐다:

```
화풍을 실사로 바꿈
  → 본처·남편·내연녀 카드는 그림체 시절 것이 그대로 다시 쓰임 (딸·변호사만 새로 그려짐)
  → 그 그림체 카드를 참조로 컷 그림을 그림 → 실사 지시문 + 그림체 그림이 섞임
  → 그 두 장 사이를 이은 영상이 앞은 실사, 뒤는 그림체
```

- 다시 쓸 수 있는 것 **옆에는 지문(무엇으로 만들었는가)을 적어 둔다** — 규칙은 `src/reuse.py` 한 곳
- 지문에는 **그때 쓴 재료 전부**를 넣는다: 지시문뿐 아니라 **참조로 넣은 그림의 속내용까지**
- 지문이 같으면 그대로 쓰고(0원), 다르거나 없으면 다시 만든다
- **날것 건너뛰기(`파일이 있으면 continue`)를 새로 쓰지 않는다** — `pair_check` 가 빨간불을 켠다

## 0-3-4. ⭐⭐⭐ 90초 한 편 (2026-08-27 확정 — "90초로 만들어")

> 16화는 한 화에 사건이 하나뿐이라 시청자가 "그래서 뭔데" 를 16번 기다려야 했다.
> 한 편이면 **5초 만에 32억**이 나온다. 16화 대본은 남겨 두되, 지금 미는 것은 이쪽이다.

| | 16화 | **90초 한 편** |
|---|---|---|
| 대본 | `data/series/S001_story.py` | `data/series/S90_story.py` (23컷) |
| 만드는 도구 | `tools/rewrite_story.py` | `tools/build_short90.py` |
| 조립 | `src/shorts.py` (띠 배치) | `src/short90.py` (세로 꽉 채움) |
| 화면 | 16:9 → 4:3 띠 | **9:16 통째** + 아래 자막 |
| 값 | 한 화 약 2,500원 | **처음 한 번 약 3,800원** (23컷 전부) |
| 단추 | 4. 영상 만들기 | **4-2. 90초 한 편 만들기** |

- ⭐⭐⭐ **그림과 영상이 섞인다.** 운영자: "이미지는 중간중간 섞여 있고 동영상도
  있어야 돼." 전부 그림이면 슬라이드쇼다. **말하는 컷은 영상**이어야 한다
- ⭐⭐⭐ **컷 프롬프트에 옷·얼굴을 적지 않는다.** 그건 인물 그림(레퍼런스)이
  잡는 몫이다. 또 적으면 두 지시가 싸워 **컷마다 옷이 바뀐다.**
  16화 쪽은 `series.wear_bait` 가 막고 있었는데 90초 편을 새로 만들면서
  그 검사를 안 붙여 그대로 새어 나갔다 (2026-08-27 손님이 잡아 주셨다).
  → `tools/short90_test.py` 가 이제 90초 편 프롬프트에도 같은 검사를 돌린다
- 컷 스물세 개 **전부** 영상 프롬프트가 있다(`S90.json` 의 `veo`). 관리자 페이지
  [90초 한 편 — 컷별 영상] 에서 컷마다 프롬프트를 복사해 제미나이로 만들고,
  만든 mp4 를 그 컷에 올린다. **올린 컷만 영상**, 안 올린 컷은 그림
- 값은 올린 만큼 준다 — 영상으로 올린 컷은 그림을 안 그린다(컷당 133원 절약).
  운영자: "비오로 하기 돈아까우니까" → Veo 를 **우리가 부르지 않는다**
- **소리를 누가 내는가**가 컷마다 갈린다
  · 대사 컷 + 올린 영상 → **그 영상 안의 목소리** (우리 목소리를 얹으면 입이 어긋난다)
  · 나레이션 컷 → 늘 **우리 나레이션** (영상을 올려도 그 소리는 안 쓴다)
  · 영상이 없는 컷 → 그림 + 우리 목소리
- 대사 컷에 영상을 올리면 **컷 길이는 그 영상이 정한다** (말이 잘리면 안 되니까).
  그래서 프롬프트에 적힌 초(4·6·8) 그대로 만들어야 전체 길이가 안 늘어난다
- 자막 칸은 `y 1300~1620`. **1620 아래는 절대 안 쓴다** — 유튜브 쇼츠 단추가 덮는다
- 만든 것(카드·그림·소리)은 릴리스에 보관하고 **지문**으로 판단한다 (`src/reuse.py`).
  대본 한 줄을 고치면 그 컷만 다시 만들어진다
- 검사: `python3 tools/short90_test.py` — 돈 한 푼 안 쓰고 **실제로 조립까지** 해 본다

---

## 0-3-5. ⭐⭐⭐ 돈이 저절로 새지 않게 (2026-08-27 확정)

> 손님: **"우리가 안 쓰는 기능인데 돈이 세면 안 되잖아. 그렇지?"**

- **돈 쓰는 워크플로는 단추(workflow_dispatch)로만 돈다.** `schedule:` · `push:` ·
  `workflow_run:` 금지. 특히 `.trigger/` 표식 파일로 도는 길은 **막았다** —
  그 파일이 딴 커밋에 딸려 들어가기만 해도 수천 원이 나갔다
- 검사: `python3 tools/auto_spend_check.py` (매 푸시 자동). 유료 열쇠를 env 에
  넣은 워크플로에 자동 실행이 하나라도 있으면 빨간불
- 자동으로 도는 것은 이제 **하나도 없다** (`collect` · `stats` 주간 실행도 껐다)
- 돈을 막는 겹은 셋이다 — 한 번 실행 한도(3,000원) · 한 달 한도(25,000원) ·
  장부(`state/spend.json`). 이건 `tools/money_guard_test.py` 가 눌러 본다

## 0-3-6. ⭐⭐⭐ 관리자 화면은 '지금 절차' 만 (2026-08-27 확정)

> 손님: **"우리가 지금 절차에서 정한 거 외에는 일단 다 감춰놓거나 삭제하고"**

지금 절차 = **90초 한 편** — ① 인물 그림 → ② 컷별 영상 → 만들기 → 보기.

| 화면에 보이는 것 | 감춘 것 |
|---|---|
| 만든 영상 · 0. 열쇠 점검 · 90초 한 편 ①② | 모으기 · 대본 · 다듬기 · 영상(16화) · 성과 · 소재 대기열 · 지난 수집 · 최근 실행 |

- `admin/worker.js` 의 `const SIMPLE = true` 하나로 갈린다. `false` 로 바꾸면 옛 화면이 돌아온다
- 안 쓰는 단추는 `WORKFLOWS` 에서 `hidden: true`. **지우지 않는다** — 되돌릴 수 있어야 한다
- 검사 둘 — `check_admin.mjs` 가 ⑴ 보이는 단추가 지금 절차뿐인지 ⑵ **감춘 것도 여전히 성한지**
  (SIMPLE 을 꺼서 전부 그려 본다) 확인한다

---

## 0-4. ⭐⭐⭐ 드라마 문법 (2026-08-25 확정 — 광고처럼 쓰지 않는다)

> 운영자: **"자극성만 신경쓰니까 씬 간 연결성과 스토리 전개가 제대로
> 이루어지지 않고 있어. 이건 광고가 아니고 드라마라는걸 다시 한번 생각해."**
>
> 광고는 3초마다 결정타가 필요하다. 그래서 매 컷을 "터지는 한 마디" 로 쓰면
> 화가 **광고 열여섯 편**이 된다. 드라마는 **약하게 시작해서 어긋나고 그래서
> 무슨 일이 벌어지는가**를 본다. 결정타는 그 끝에 한 번 온다.

### 여섯 가지 규칙 (전부 검사가 붙어 있다)

| # | 규칙 | 검사하는 곳 |
|---|---|---|
| ① | **한 화 = 한 사건.** 3컷은 시작 → 어긋남 → 결과 | 사람이 본다 |
| ② | **이음** — 이 화의 `leaves` 가 다음 화의 `because` 와 **글자 그대로 같다** | `story_check.py` (버림) |
| ③ | **정보는 원수가 아니라 세상에서 온다** — 전화·창구·변호사·우편 | 사람이 본다 |
| ④ | **상대를 바꾼다** — 같은 조합이 세 화 넘게 잇달면 안 된다 | `story_check.py` (버림) |
| ⑤ | **조용한 화(`quiet`)를 섞는다** — 하나는 있고 절반은 넘지 않는다 | `story_check.py` (버림) |
| ⑥ | **컷당 대사 2~3개.** 사람은 10초에 네 마디를 안 한다 | `series.py` 음절 검사 |

### 하지 않는 것

- **모든 화를 물음표로 끝내지 않는다.** 예전 규칙이 그것을 강제했는데, 16화 중
  14화가 물음표로 끝나면서도 그중 4곳은 다음 화가 그 질문을 무시했다.
  물음표는 이어짐을 **흉내만** 낸 것이다. 이제 물음표는 **알림**일 뿐이고,
  진짜 이어짐은 `leaves` = `because` 로 검사한다.
- **원수가 다음 단서를 알려 주지 않는다.** ("보험사에도 가보시든가요" 금지)
- **만날 이유가 없는 곳에서 마주치지 않는다.** 은행 앞·보험사 앞에 상대가
  서 있을 까닭이 없다. 마주치는 곳은 장례식장·법정·법원처럼 **둘 다 가야 할
  이유가 있는 곳**뿐이다.

### 시간 — 3막으로 묶는다 (2026-08-26 추가)

> 운영자: **"각 화마다 시간적 순서가 너무 많이 바뀌고 쌩뚱맞아서 연결이 안 되는 느낌이야.
> 이거 판결문을 드라마로 만드는 게 불가능한 건가?"**
>
> 불가능하지 않다. 원인은 **판결문의 시간 순서를 그대로 따라간 것**이었다.
> 재 보니 화 사이 간격이 **0개월에서 34개월까지** 널뛰었다.

| 막 | 화 | 때 | 이름 |
|---|---|---|---|
| 1막 | 1~5 | 2012~2014 | 배신 |
| 2막 | 6~11 | 2017 | 죽음 |
| 3막 | 12~16 | 2017~2020 | 법정 |

- **큰 시간 점프는 막이 바뀔 때만.** 막 안에서는 12개월을 못 넘는다
  (`story_check.py` 의 `ACT_GAP_MAX` 가 검사한다 — 넘으면 버린다)
- 화마다 `act` 를 적는다. 안 적으면 검사가 버린다

### 시점 — 아내로 고정한다

- **모든 화에 아내가 나온다.** 아내가 빠진 화는 검사가 버린다(`LEAD`)
- 예전엔 4·6화에 아내가 없었다(남편·내연녀만 나오는 화). 시청자가 아내를
  따라오다가 거기서 두 번 튀었다 — "쌩뚱맞다" 의 가장 큰 원인이었다
- 시청자만 먼저 아는 재미(극적 아이러니)는 포기한다. **아내와 같은 순서로
  알게 되는 것**이 30초짜리에서는 더 중요하다

### 화면이 시간을 알려 준다

| 무엇 | 어디 | 언제 |
|---|---|---|
| 때 도장 `3년 뒤 · 2017년 1월` | 자막 칸 오른쪽 위 | **첫 컷 내내** |
| 지난 줄거리 `지난 화 · …` | 영상 칸 맨 위 어두운 띠 | 앞머리 2.8초 |

- 도장은 **절대 시각이 아니라 간격**을 앞에 적는다. 사람은 "2016년 여름" 이
  아니라 "2년 뒤" 로 시간을 느낀다 (`rewrite_story.py` 의 `stamp_of`)
- 지난 줄거리는 화마다 적어 두고도 **그리는 코드가 없어서 화면에 안 나왔다.**
  쇼츠는 앞 화를 안 보고 들어오는 사람이 대부분이다

### ⭐⭐⭐ 대사 다섯 규칙 (2026-08-26 — 어기면 검사가 버린다)

> 운영자: **"대사도 더럽게 재미없어요. 말 뚝뚝 끊어먹고 어색하고 컴퓨터처럼
> 얘기하지 마. 사람이 얘기하는 것처럼 해야지."**
>
> 내가 두 가지를 저질렀다. ① 참고 영상의 자막이 4~9글자로 보이길래 **문장
> 자체를 그렇게 잘랐다** — "그리고 판결 난 그날 / 재산을 넘겼습니다" 처럼
> 누가 누구에게가 사라졌다. ② **대사에 금액·날짜를 실어** 보고서 낭독으로
> 만들었다 — "매달 2천만 원씩 보험료를 부었더군요".

| # | 규칙 | 왜 |
|---|---|---|
| ① | **문장을 토막 내지 않는다** | 화면에 4~9글자씩 나눠 띄우는 건 `shorts.py` 의 `sub_chunks` 가 알아서 한다. 대본에는 **온전한 문장**만 쓴다 |
| ② | **대사는 사실을 나르지 않는다** | 한 대사에 금액·날짜는 **하나까지**. 그 이상은 해설이 진다 |
| ③ | **한 대사는 두 문장까지** | 세 문장 넘으면 사람이 아니라 연설이다 |
| ④ | **판결문 말투 금지** | "~더군요" "~하였" "~에 대하여". **변호사·재판장만** 봐준다 |
| ⑤ | **화마다 군말·되묻기가 하나는 있다** | "야" "아니" "근데" "왜" "무슨" 이 없으면 컴퓨터가 말하는 것이다 |

**잡지 않는 것** — 사람은 실제로 이렇게 말한다.
- **되풀이 강조**: "이십 년이야. 이십 년을 같이 살았어." (같은 숫자 두 번은 하나로 센다)
- **날짜 한 덩어리**: "이천십삼년 팔월 구일이야." (숫자 셋이 아니라 사실 하나)
- **흐린 말끝**: "그이가 왜 당신 집에서…" (말줄임표가 있으면 일부러 흐린 것)

**⑥ 글로 쓰는 말을 입으로 시키지 않는다** (2026-08-26 운영자)

> "사람이 말할 때는 '니가' 하지, '네가' 라고 하지 않아. 그건 구어체가 아니고
> 문어체야. 구어체로 똑바로 적어."

| 글로 쓰는 말 | 입으로 하는 말 |
|---|---|
| 네가 / 네 아빠 | **니가 / 니 아빠** |
| 너의 | 니 |
| 무엇을 / 무엇이 | 뭘 / 뭐가 |
| 하지 아니하다 | 안 한다 |

⚠️ 대답하는 **"네"** 는 건드리지 않는다 ("네, 제가 아내인데요").

### ⭐ 때(날짜) — 판결문 날짜를 그대로 쓰지 않는다

> "판례에 있는 날짜 그대로 쓰는 거 아니지? 오해받을 수 있으니깐 조금 바꿔라.
> 그리고 일까지 표시하지 말고 몇 월인지 정도까지만 얘기하고."

- 실제 날짜에서 **한 달씩 옮긴다** (기각 2013년 9월 · 사망 2017년 2월 · 판결 2020년 7월)
- **일(日)은 화면에도 대사에도 안 쓴다.** 월까지만
- 법원 이름도 안 밝힌다 — "실제로 있었던 사건입니다" 로 닫는다
- 반전('기각 당일에 도장을 찍었다')은 숫자가 아니라 **"같은 날" 이라는 말**이 진다

**⑦ 높임말은 사람 쌍마다 처음부터 끝까지 같다** (2026-08-26 운영자)

> "왜 자꾸 아내는 내연녀한테 존댓말 했다가 반말 했다가 일관성이 없어.
> 특별히 바뀌어야 될 상황이 아니잖아."

세어 보니 **세 쌍이 섞여 있었다.**

| 쌍 | 있어야 할 것 | 섞인 곳 |
|---|---|---|
| 아내 → 내연녀 | **반말** | 16줄 중 한 줄만 존댓말 |
| 내연녀 → 아내 | **존댓말** (차갑게 거리 두는) | 두 줄이 한 대사 안에서 반말 |
| 아내 → 딸 | **반말** | 창구 직원에게 한 말이 잘못 묶임 |

| 쌍 | 높임말 |
|---|---|
| 아내 → 남편 · 딸 · 내연녀 | 반말 |
| 아내 → 변호사 | 존댓말 |
| 내연녀 → 아내 | 존댓말 |
| 딸 → 아내 | 반말 |
| 변호사 → 아내 | 존댓말 |

**화면 밖 상대**(전화·은행 창구)에게 하는 말은 이야기 데이터에 `aside` 로 적어
검사에서 뺀다. 예: 8화 1컷 아내가 창구 직원에게 "이게 다예요?"

**⑧ 정황에 맞는 말인가** (검사가 못 잡는다 — 사람이 본다)

> "장례식장은 내연녀가 지키고 있었을 텐데. '우세요' 뭔 말이야."

장례식장 상주는 **아내**다. 내연녀는 찾아온 불청객이다. 그런데 "우세요" 라고
쓰면 내연녀가 조문객을 맞는 주인이 된다. **그 자리에서 그 사람이 할 수 있는
말인지**를 매번 확인한다. 검사기가 못 잡는 유일한 규칙이라 사람이 본다.

### ⚠️ 검사가 사람 말을 잡으면, 검사가 틀린 것이다

이 검사는 만들자마자 **멀쩡한 대사를 다섯 번 잡았다.** 사람은 실제로 이렇게 말한다.

| 검사가 잡은 것 | 실은 |
|---|---|
| "이십 년이야. 이십 년을 같이 살았어." | 되풀이 = 강조 |
| "이천십삼년 구월이야." | 숫자 셋이 아니라 날짜 하나 |
| "그이가 왜 당신 집에서…" | 일부러 흐린 말끝 |
| "네, 제가 아내인데요." | 대답하는 '네' |
| "야. 이겼다고 좋아하지 마. 나 아직 안 끝났으니까." | '야' 는 감탄사지 문장이 아니다 |

다섯 가지 전부 **안 잡도록 고치고 자기시험에 넣었다.** 대사를 검사에 맞추지 말고,
검사를 사람 말에 맞춘다.

**말버릇은 사람마다 다르다** (`talk_check.py` 가 겹치면 알려 준다)

| 인물 | 말버릇 |
|---|---|
| 아내 | 되묻는다. 구체적인 일상어(밥·빨래·등록금). "~잖아" "~라고" |
| 남편 | 짧게 끊고 명령형. "~고." "찍어" "말해" |
| 내연녀 | 존댓말인데 도발적. "~거든요" "~든가요" |
| 딸 | 짧게 묻는다. "~야?" "~잖아" |
| 변호사 | 유일하게 문어체가 허용된다. "~습니다" |

검사: `python3 tools/talk_check.py` (0원, 매 푸시마다 자동)

### 등장인물 (상한 5명 — `series.CHARS_MAX`)

아내 · 남편 · 그 여자 · **딸** · **변호사**.
딸이 있어야 조용한 화가 되고, 변호사가 있어야 아내가 금액을 **스스로 발견**한다.
기준 그림 프롬프트는 **손으로 안 적는다** — `src/charsheet.py` 가 짓는다.

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 채널명 | 판결극장 |
| 부제 | 실제 판결을 재구성한 드라마 |
| 타깃 | 한국 50~60대 |
| 롱폼 길이 | **12분** (중간광고 요건 충족) |
| 쇼츠 | 편당 **3편** (35~50초) |
| 발행 | 롱폼 주 4편 + 쇼츠 주 12편 |
| 운영자 시간 예산 | **주 2시간** |
| 운영자 코딩 수준 | **비전문가.** 전문용어는 첫 사용 시 풀네임 + 간략 설명 필수 |

### ⚠️ 최우선 제약

**운영자는 아이폰 15 Pro만 사용한다. 아이패드 A16이 보조다. PC는 없다.**

| 금지 | 이유 |
|---|---|
| 도커 컨테이너 빌드 | 아이폰에서 불가 |
| Cloud Run 배포 | 컨테이너 빌드 필요 |
| 명령줄 도구 로컬 실행 | 터미널 없음 |
| 로컬 파일 처리 전제 | 로컬 환경 없음 |

**모든 실행은 GitHub Actions 안에서 일어난다.** 운영자는 버튼을 누르고 결과를 확인한다.

### ⭐ 핵심규칙 — 운영자가 고를 것은 **반드시 선택 메뉴로** 보여준다

운영자에게 **고르라고 할 것이 하나라도 있으면, 글로 늘어놓지 말고 선택 메뉴로 띄운다.**
예외 없다.

| 이렇게 하지 마라 | 이렇게 한다 |
|---|---|
| "A·B·C 중 원하시는 것을 알려주십시오" 를 글로 적기 | 선택 메뉴를 띄워 **탭 한 번**으로 고르게 한다 |
| 본문 여기저기에 물음표를 흩어 놓기 | 고를 것을 **한곳에 모아** 한 번에 묻는다 |
| 선택지 이름만 나열하기 | 선택지마다 **무슨 일이 벌어지는지 · 얼마가 드는지**를 붙인다 |

이유 — 운영자는 아이폰으로 본다. 긴 글 속에서 선택지를 찾아 타이핑으로 답하는 것은
**PC 앞에 앉은 사람의 방식**이다. 이 프로젝트의 운영자에게는 탭 한 번이 맞다.
실제로 선택지를 글로만 늘어놓았다가 *"내가 선택할 사항은 반드시 정리해서 선택 메뉴
형태로 보여줘"* 라는 지적을 받았다.

**추천안이 있으면 맨 위에 두고 `(추천)` 을 붙인다.** 고민 없이 고를 수 있어야 한다.

### ⭐ 핵심규칙 — **망가진 그림은 빠진 그림보다 나쁘다**

2026-08-13, 목 아래만 남은 인물 셋이 그대로 방송에 나갔다.
운영자가 눈으로 보고 *"미친놈아 이건 목이 잘렸잖아"* 라고 할 때까지 아무도 몰랐다.

원인은 검사의 **모양**에 있었다. 그때까지의 검사는 전부 **"있는가 없는가"** 만 봤다.

| | 그때 | 결과 |
|---|---|---|
| 그림이 **빠졌다** | 무결성 검사가 막았다 | 영상이 안 만들어진다 → 안전 |
| 그림이 **망가졌다** | 아무도 안 봤다 | 파일이 있으니 통과 → **조용히 방송된다** |

**있는지 세지 말고, 쓸 만한지 재라.** 파일 개수는 품질이 아니다.

적용 (어기지 말 것)
1. 새 에셋을 만드는 코드에는 **"제대로 나왔는가" 검사를 같이 만든다.**
   개수 검사만 있으면 그 코드는 미완성이다.
2. 그 검사는 **[영상 만들기]가 부르고 실패하면 멈춘다.** 보고만 하면 소용없다.
   (`tools/head_test.py` → `produce.yml` · `selfcheck.yml` 둘 다에 걸려 있다)
3. 값이 안 드는 확인(그림을 보는 눈에게 묻기)이 이미 있으면 **거기에 얹는다.**
   포즈 이름을 확인하는 그 호출에서 "목 잘린 것 있나" 도 같이 묻는다 — 0원이다.

### ⭐ 핵심규칙 — **같은 실수가 반복되는 진짜 까닭 다섯 가지**

운영자 질문: *"왜 자꾸 이런 일이 반복되는 거야?"* — 기록을 뒤져 원인을 찾았다.
운영자 잘못이 아니고 운도 아니다. **고치는 방식이 틀렸다.** 다섯이다.

#### ① 길을 하나씩 막았다 — 그래서 남은 길로 계속 돌아왔다

시계 초침 소리를 세 번 지웠는데 세 번 다 살아났다.

| 언제 | 무엇을 지웠나 | 왜 살아났나 |
|---|---|---|
| 08-09 | 소리 **파일** | 만드는 법(`SFX_RECIPE`)이 남아 버튼 한 번에 재생성 |
| 08-13 | **만드는 법** | 받아오는 법(`get_sfx.PROFILES`)이 남아 다운로드로 부활 |
| 08-13 | **받아오는 법** | ← 이제야 다 막힘 |

**소리 하나가 생기는 길은 다섯이다.** 합성 · 다운로드 · 낱말 매칭 · 배경 매칭 ·
대본이 직접 지정. 그때그때 눈에 띈 하나만 막으니 계속 돌아왔다.

**규칙: 무언가를 빼려면 "그것이 생길 수 있는 길"을 전부 세어 한 번에 막는다.**
그리고 **그 목록 자체를 검사로 만든다** (`tools/sfx_test.py` [5-2] 가 다섯 길을
전부 훑는다). 실제로 이 검사를 만들자마자 놓친 구멍 3개가 즉시 나왔다.

#### ② 규칙만 적고 지켜졌는지 재지 않았다

운영자 지적: *"애시당초 선은 마젠타로 긋고 배경을 초록으로 만들었으면 이런 일이
없었을 텐데"* — **색은 처음부터 그렇게 하고 있었다.** 마젠타 선 · 초록 배경이 맞다.

빠진 것은 색이 아니라 **어디에 그으라는 말**이었고, 더 빠진 것은
**그 말이 지켜졌는지 재는 일**이었다. 그래서 선이 인물 머리 위에 겹쳐 그려진
시트가 아무 경고 없이 다음 단계로 넘어갔고, 거기서 목이 잘렸다.

**규칙: 프롬프트에 규칙을 적었으면, 그 규칙이 지켜졌는지 재는 코드도 같이 만든다.**
못 재는 규칙은 규칙이 아니라 희망이다.
(`assets_gen.lines_touch_figures()` → `tools/head_test.py` 가 매번 잰다)

#### ③ 코드만 늘리고, 그 코드가 딛고 설 바닥은 안 봤다

2026-08-14 시범 1장. 그림은 **멀쩡히 다 그려졌고(265원 나갔고)**, 바로 그 뒤
검사기가 `ModuleNotFoundError: No module named 'numpy'` 로 넘어졌다.
새로 만든 `sheet_gate.py`·`char_sheet.py` 는 numpy 로 픽셀을 세는데,
`build-assets.yml` 의 '도구 준비' 는 `Pillow` 만 깔고 있었다.

**돈이 나간 뒤에 넘어지는 것**이 이 실수의 성질이다. 코드에 `import` 한 줄을
더할 때마다 사람이 워크플로 열다섯 개를 뒤져 pip 줄을 고칠 리가 없다.

**규칙: 코드가 무엇을 쓰는지 사람이 기억하지 말고, 코드가 코드를 읽어 대조하게 한다.**
(`tools/deps_test.py` — 워크플로가 부르는 .py 를 따라 들어가 import 를 모으고
pip 줄과 맞춰 본다. 넣자마자 같은 지뢰 둘을 더 찾았다: `script.yml` 의
Pillow·numpy 누락은 **대본값 3,000원이 나간 뒤** 터질 자리였다.)

#### ④ 같은 숫자를 두 곳에 따로 적고, 서로 맞는지 안 봤다

시범 시트가 넷에서 걸렸다(간격 88 · 가장자리 64 · 밴드 92px 침범 · 폭 840).
프롬프트에 적은 숫자와 검사기가 재는 기준이 **애초에 서로 안 맞았는데**,
그 둘을 맞춰 보는 곳이 어디에도 없었다. 그림을 뽑아 봐야만 알 수 있으니
**확인 한 번에 265원**이 들었다.

**규칙: 값이 드는 확인은 종이 위에서 먼저 푼다.**
프롬프트 숫자를 코드가 직접 읽어, 모델이 ⓐ 시킨 대로 그렸을 때와
ⓑ 크게 그렸을 때(실측 1.23배) **양쪽 끝**을 다 넣고 검사 기준을 통과하는지
산수로 확인한다. (`tools/gate_test.py` ⑦ — 넣자마자 전신 시트 폭 640 이
1.23배면 가장자리 85px 밖에 안 남는 것을 잡아, 265원을 또 날릴 뻔한 것을 막았다.)

#### ⑤ 합격한 것을 한 번도 못 본 채 **채점표부터 썼다** (2026-08-14 · 가장 큰 것)

하루에 좋은 시트를 **세 번** 떨어뜨렸다. 그림은 세 번 다 멀쩡했고 틀린 것은 검사기였다.

| 내가 정한 기준 | 실제 | 근거 |
|---|---|---|
| 하단 700px 을 비워라 (제미나이 로고 자리) | 시트 9장에 **로고 없음** | 짐작 |
| 인물 폭 ≤820 · 키 ≤1250 | 진짜는 888 · 1264 | 손으로 적음 |
| 초록 ≥50% | 진짜는 46~58% | "많아야 한다" 고 가정 |
| 인물 간격 ≥120 | 전신은 80이 정상(걷는 자세) | 얼굴 기준을 전신에 씀 |
| 맨 위가 좁아야 머리 | 주저앉으면 등이 맨 위 | 선 자세만 보고 만듦 |

**규칙 1 — 합격 표본이 0장이면 그 자는 아직 자가 아니다.**
그 종류의 시트가 `assets/sheets/` 에 한 장도 없으면 `sheet_gate` 는 **막지 않고
알리기만 한다**(`proven()`). 사람이 한 장 받아들인 뒤부터 진짜로 막는다.

**규칙 2 — 불합격이 나오면 숫자보다 그림을 먼저 본다.**
사고 셋이 전부 "검사기가 불합격이라 해서 그림을 안 열어 본" 것이었다. 이제
걸린 시트는 `assets/sheets/bad/preview/*.jpg` 로 **작은 미리보기가 같이 남는다**
(값 0원, 폰에서 한 번 누르면 보인다).

**규칙 3 — 직접 잰 것만 불합격, 어림으로 대신 잰 것은 경고.**
붙었나 = G3(개수)·G7(무리)·G4(간격)가 직접 잰다. 잘렸나 = G5 가 직접 잰다.
크기 상한은 그것을 대신 보던 어림일 뿐이라 **경고**로 내렸다.

### ⭐ 핵심규칙 — **의심스러우면 지우지 말고 이어 붙인다**

같은 사고의 직접 원인은 "격자선을 지우는" 코드였다. 모델은 **머리 꼭대기 위로
격자선을 긋는다.** 그 선을 지우자 머리가 같이 지워졌다.

- 잘못 **이어 붙이면** → 작은 막대 토막이 남는다 (보기 싫다)
- 잘못 **지우면** → 사람의 머리가 없어진다 (방송 못 한다)

**둘의 무게가 다르다.** 지울지 말지 헷갈리면 남긴다.

### ⭐ 핵심규칙 — **자르는 코드로 못 살릴 그림은 그리는 단계에서 막는다**

목 잘림을 슬라이서로 고쳐 보려고 세 번 시도했고 **세 번 다 다른 데가 망가졌다**
(옆 사람이 딸려 오거나, 포즈를 잃거나, 통째로 합쳐지거나).

시트가 애초에 잘못 그려져 있었다 — 줄마다 칸 수가 다르고(위 3칸·아래 4칸),
칸 사이에 여백이 없고, 격자선이 인물 위를 지났다.
**그런 그림은 어떻게 잘라도 안 된다.** 프롬프트에 못 박아야 한다:
모든 줄이 같은 칸 수 · 인물 둘레에 여백 · 선이 인물에 닿지 않는다.

---

## 2. 시스템 구성

```
┌───────────────────────────────────────────────────────┐
│ GitHub 비공개 저장소 1개 — 모든 것의 중심                │
│                                                        │
│  /prompts               ★핵심자산                       │
│    script_gen.md          대본 생성                     │
│    script_eval.md         대본 평가                     │
│    drama_gate.md          판례 드라마성 평가             │
│    shorts_gen.md          쇼츠 대본                     │
│  /src                   파이프라인 코드                  │
│  /state                                                │
│    episodes.json          회차 상태 (핵심)              │
│    queue.json             대기열                        │
│    calendar.json          발행 일정                     │
│    rejected.json          개선 실패 기록 (학습용)        │
│  /data                                                 │
│    cases/{번호}.json      판례 원문 (약 28MB)           │
│    scripts/{회차}.json    완성 대본                     │
│  /assets                                               │
│    char/{코드}/*.png      캐릭터 컷아웃 126개 (알파)     │
│    bg/*.jpg               배경 30장                     │
│    prop/*.png             소품                          │
│    bgm/*.mp3              음악 8트랙                    │
│    amb/*.mp3              앰비언스 5종                  │
│    sfx/*.mp3              효과음 10종                   │
│    sheets/*.png           원본 시트 (보관)              │
│  /.github/workflows                                    │
│    build-assets.yml       에셋 생성·가공 (수동)         │
│    collect.yml            판례 수집 (크론 주1회)        │
│    script.yml             대본 생성 + 평가 + 개선       │
│    produce.yml            렌더링 + 업로드 (통합)        │
│    stats.yml              성과 수집 (크론 주1회)        │
└───────────────────────────────────────────────────────┘
                            │
                            ▼
                 YouTube (비공개 → 검수 → 공개)
```

### 저장 원칙

**이미지·음악·글자는 전부 GitHub. 완성 영상은 어디에도 저장하지 않는다.**

- 영상은 Actions 실행 안에서 만들어 **곧바로 유튜브 비공개로 업로드**한다. 실행이 끝나면 작업 공간은 자동 삭제된다
- 검수는 아이폰 유튜브 앱에서 한다 (비공개 영상은 본인만 보임)
- **MP4를 GitHub에 커밋하면 안 된다.** 편당 200~500MB이고, 지워도 이력에 영구히 남아 저장소가 되돌릴 수 없이 커진다

| 대상 | 형식 | 이유 |
|---|---|---|
| 인물 컷아웃 | **PNG** | 투명 배경(알파 채널) 필요. JPG는 투명을 저장할 수 없다 |
| 배경 | **JPG** | 맨 아래 깔리는 판이라 투명 불필요. 블러 처리하므로 화질 손실 안 보임. PNG 대비 용량 1/7 |

```
저장소 예상 용량
  이미지 에셋   약 110MB
  음악·소리     약  10MB
  판례 원문     약  28MB
  ───────────────────────
  합계         약 148MB   (GitHub 권장 상한 1GB)
```

### GitHub Actions를 쓰는 이유

| 항목 | 내용 |
|---|---|
| FFmpeg | ubuntu 러너에 **미리 설치됨.** 컨테이너 불필요 |
| Python·Pillow·rembg | 워크플로에서 설치 |
| 무료 한도 | 비공개 저장소 월 2,000분 |
| 실제 사용량 | 롱폼 10분 + 쇼츠 3분 = 편당 약 13분 × 월 16편 = **약 210분 (한도의 11%)** |
| 크론 | `schedule` 자체 지원 |
| 수동 실행 | `workflow_dispatch` → 아이폰 GitHub 앱 버튼 |

- **주의**: 비공개 저장소의 `schedule` 워크플로는 저장소 활동이 60일간 없으면 자동 비활성화된다. 주 1회 커밋이 발생하므로 문제없다

---

## 3. 회차 상태 관리 (필수)

`state/episodes.json` 이 파이프라인의 두뇌다. 이것 없이는 재실행 시 중복·누락이 발생한다.

```json
{
  "EP001": {
    "case_id": "622261",
    "gate_score": 78,
    "stage": "published",
    "script_score": 86,
    "revise_count": 1,
    "case_type": "유류분",
    "longform_id": "유튜브영상ID",
    "shorts": ["ID1", "ID2", "ID3"],
    "published_at": "2026-08-05",
    "shorts_published": ["2026-08-06", "2026-08-07", "2026-08-09"]
  }
}
```

**단계(stage) 값**

```
selected → scripting → evaluated → rendering
  → uploaded_private → approved → published
```

- 각 워크플로는 **자기가 처리할 단계의 회차만** 집어간다
- 중간에 실패하면 그 단계에 머물러 있어 다음 실행에서 재시도된다
- **모든 워크플로는 시작 시 이 파일을 읽고, 끝나면 갱신한다**

---

## 4. 2단 판정 체계 (게이트와 개선은 별개다)

| | **게이트 (선정)** | **개선 루프 (대본)** |
|---|---|---|
| 목적 | **제작 여부 결정** | **재미 강화** |
| 대상 | 판례 원문 | 완성된 대본 |
| 폐기 | **있음** | **없음** |
| 위치 | 수집 직후 | 대본 생성 직후 |

**설계 근거**: 걸러낼 지점은 가장 싼 단계여야 한다. 또한 게이트를 선정 단계에 두면 "소재가 나빴는가 / 대본 생성기가 나빴는가"가 분리되어 어느 쪽을 고칠지 알 수 있다.

### 4-1. 게이트 — 3단 선정

**1차: 기계 하드 배제**

- `사건종류코드` ≠ `400101`(민사)
- `데이터출처명` = `국세법령정보시스템` (세금 사건이 민사로 위장 유입됨)
- `판결유형` ≠ `판결` (결정·명령은 사실관계 부족)
- 본문 3,000자 미만
- 사건명 금지어: 과세, 부과처분, 환급, 양도소득, 증여세, 상속세, 취득세, 종합소득, 아동, 청소년, 강간, 강제추행, 성폭력, 음란, 성매매, 선거, 정당, 정치자금, 특허, 상표, 저작권침해, 산업재해

**2차: 기계 가점 (대본화 가능성)**

| 조건 | 점수 |
|---|---|
| 지방법원(1심) | +30 |
| 고등법원(2심) | +20 |
| 대법원 | +5 |
| `인정사실`/`기초사실`/`사안의 개요` 존재 | +25 |
| 가족 관계 단어 (단어당 3점) | 최대 +15 |
| 선고 5년 이내 | +10 |
| 억 단위 금액 등장 | +10 |
| 본문 2만자 초과 | +10 |

**3차: LLM 드라마성 평가 (100점) — 프롬프트: `drama_gate.md`**

| 항목 | 배점 | 판정 기준 |
|---|---|---|
| 갈등 선명도 | 20 | 누가 누구에게 무엇을 했는지 한 문장으로 요약되는가 |
| 배신 구조 | 20 | 가족 간 신뢰를 배반한 행위가 있는가 |
| 반전 요소 | 15 | 숨겨진 사실, 시간 역전, 예상 밖 전개 |
| 감정 이입 대상 | 15 | 50~60대가 자기 처지로 읽을 피해자가 있는가 |
| 결말 카타르시스 | 15 | 판결이 명확히 한쪽 손을 들어줬는가 |
| 금액 규모 | 8 | 1억 원 이상 |
| 서술 충분성 | 7 | 12분을 창작으로 채울 기반이 있는가 |

**통과선 60점.** 낮게 잡은 근거: 소재 은행 약 400건에서 연 208편이 필요하므로 통과율 50% 이상이어야 물량이 유지된다. 통과선을 90점으로 올리면 통과율이 10%로 떨어져 주 4편이 불가능해진다.

**대기열은 점수 순으로 정렬**한다. 물량이 여유로울 때는 높은 것부터 쓰고, 부족할 때 자연히 낮은 점수를 쓴다. 게이트와 우선순위를 동시에 얻는다.

**소재 유형 분산**: 같은 사건 유형(예: 유류분)을 **연속 2편 초과 금지.** 점수가 높아도 3편째는 다른 유형으로 넘긴다.

### 4-2. 개선 루프 — 대본 평가 (폐기 없음)

**프롬프트: `script_eval.md` / 100점, 목표 80점 / 최대 3회**

| 항목 | 배점 | 세부 |
|---|---|---|
| **A. 3초 관문** | **30** | 첫 문장에 갈등 존재 10 / 배경 설명 배제 10 / 미완결로 붙잡기 10 |
| B. 30초 관문 | 20 | 억울한 주체 명확 10 / 걸린 것(금액·관계) 제시 10 |
| C. 중반 유지 | 20 | 7초 이상 무정보 구간 없음 8 / 막별 신정보 7 / 감정 교차 5 |
| D. 결말 만족 | 15 | 권선징악 6 / 판결 논리 이해 5 / 금액 특정 4 |
| E. 구독·댓글 | 15 | 자기 경험 연결점 6 / 질문 마감 5 / 다음 편 기대 4 |

**운영 규칙**

- **A가 24점 미만이면 총점과 무관하게 A만 재작성.** 3초에서 놓치면 나머지가 무의미하다
- 평가는 **반드시 별도 호출**로 한다. 같은 호출에서 하면 자기 작품을 감싸준다
- 보강은 **미달 항목만.** 전체 재작성은 잘 된 부분까지 망친다
- **3회 후에도 미달이면 폐기하지 않는다.** 최고 점수 버전으로 발행하고, 그 판례의 유형·특징을 `state/rejected.json`에 기록한다
- 이 기록이 쌓이면 **게이트 점수 기준을 스스로 보정**한다. 루프가 상위 단계로 되돌아간다

### 4-3. 쇼츠 — 편당 3편

**대본 단계에서 구간을 지정한다. 완성 영상을 잘라내지 않는다.**

잘라내면 나레이션이 문장 중간에 끊기고, 자막이 잘리고, 맥락이 없어 쇼츠 시청자가 1초에 이탈한다. 대신 대본 생성 시 LLM에게 구간 지정 + 쇼츠 전용 도입 1문장 + 전용 마무리 1문장을 함께 쓰게 한다. 렌더링 단계에서 같은 대본을 16:9와 9:16 두 크기로 출력한다.

컷아웃 방식이라 세로 변환이 거의 무료다. 인물 PNG를 9:16 틀에 재배치하고 배경만 잘라내면 된다.

| 쇼츠 | 성격 | 구간 | 감정 | 마무리 |
|---|---|---|---|---|
| 1번 | **궁금증형** | 도입 훅 또는 반전 발견 | 의문 | "결말은 본편에서" |
| 2번 | **분노형** | 상대의 뻔뻔한 대사 순간 | 분노 | "여러분이라면?" |
| 3번 | **공감형** | 1막 헌신 또는 3막 가족이 갈라서는 순간 | 먹먹함 | 관계를 묻는 문장으로 닫음 |

- 2번이 댓글 유발에 가장 강하다. 대사 하나가 댓글 수백 개를 만든다
- 감정 곡선이 의문/분노/해소로 완전히 갈리므로 조각내기로 읽히지 않는다
- **정보형 쇼츠(법률 해설)는 만들지 않는다.** 운영자가 법률 전문가가 아니므로 정확성 리스크가 크다

**쇼츠 평가 (통과선 85점, 롱폼보다 가혹)**

| 항목 | 배점 |
|---|---|
| **1초 관문** — 첫 화면·첫 소리에 이상 상황 | **40** |
| 독립 이해도 — 본편 없이 이해되는가 | 25 |
| 마무리 행동 유도 | 20 |
| 길이 적정 (35~50초) | 15 |

**⚠️ 발행 간격 분산 필수**

```
롱폼 발행일  →  쇼츠 없음
+1일         →  쇼츠 1번 (궁금증형)
+2일         →  쇼츠 2번 (분노형)
+4일         →  쇼츠 3번 (공감형)
```

- **같은 날 3편 동시 업로드 절대 금지.** 조각내기 신호가 명확해진다
- 8주 뒤 성과 데이터로 3편 유지 여부를 재검토한다. 3번이 1·2번보다 현저히 낮으면 2편으로 줄인다

---

## 5. 데이터 확보

- **법제처 국가법령정보 OPEN API.** 인증키(OC): `panryetheater`
- 목록: `https://www.law.go.kr/DRF/lawSearch.do?OC={OC}&target=prec&type=XML&query={검색어}&display=100&page={n}`
- 본문: `https://www.law.go.kr/DRF/lawService.do?OC={OC}&target=prec&type=XML&ID={판례일련번호}`
- **일일 호출 한도 미공개.** 호출 간 1.1초 간격, 하루 200회 상한 유지
- **백필(과거 전량 1회 수집)이 본체다.** 검색어 40개 × 알짜 약 10건 = 약 400건 = 2년치. 증분만으로는 물량이 안 나온다

### 확인된 응답 필드명

**목록** (`<PrecSearch>` > `<prec>`)
```
판례일련번호, 사건명, 사건번호, 선고일자, 법원명, 법원종류코드,
사건종류명, 사건종류코드, 판결유형, 선고, 데이터출처명, 판례상세링크
```

**본문** (`<PrecService>`)
```
판례정보일련번호, 사건명, 사건번호, 선고일자, 선고, 법원명, 법원종류코드,
사건종류명, 사건종류코드, 판결유형, 판시사항, 판결요지, 참조조문,
참조판례, 판례내용
```

### 실측으로 확인된 처리 함정

| 사항 | 대응 |
|---|---|
| 값이 CDATA로 감싸짐 | `<![CDATA[...]]>` 벗겨내기 |
| 본문에 `<br/>` 태그 섞임 | 개행 치환 후 태그 제거 |
| 선고일자 형식 불일치 | 목록 `2026.05.29` / 본문 `20260529` → `YYYY-MM-DD` 통일 |
| **법원명이 빈 값인 경우 있음** | 사건번호에서 추출 (예: `부산고등법원(창원)-2024-나-14544`) |
| 세금 사건이 민사로 위장 유입 | `데이터출처명` = `국세법령정보시스템` 배제 |
| **⚠️ 하급심은 `판시사항`·`판결요지`·`참조조문` 이 전부 빈 값** | **`판례내용` 만 읽는다.** 결말·금액은 `【주 문】`, 사실관계는 `1. 인정사실`, 조문은 본문에서 「민법 제○○○조」 추출. (2026-07-30 실측: 하급심 4건 전부 0자, 대법원만 채워짐 — `data/cases/README.md`) |
| 원화가 아닌 사건 존재 | 해외 자산 사건은 금액이 외화(엔화 등)로 나온다. 익명화 시 통화까지 처리 |

### 검색어 사전 (53개) — v2

```
A군 상속: 유류분, 유류분반환, 상속회복, 유언무효, 유언효력, 사인증여,
         특별수익, 상속재산분할, 상속포기, 한정승인, 상속채무
B군 재산: 명의신탁, 증여계약해제, 부담부증여, 사해행위취소,
         부당이득반환, 소유권이전등기말소, 근저당권설정말소
C군 부양: 부양료, 부양의무, 구상금, 요양병원, 성년후견, 의사무능력
D군 불륜: 상간, 상간녀, 상간남, 정조, 혼인파탄, 부정한 행위      ← 개편
E군 노년: 재혼, 사실혼재산, 사실혼해소, 유족연금
F군 가업: 가업승계, 주식증여, 명의대여
G군 기타: 친생자관계, 인지청구, 기여분, 부양청구, 차용금, 유언집행
H군 혼외자: 가액지급, 피인지자, 상속분가액, 가액반환               ← 신설
I군 확장: 제사주재자, 분묘, 유골, 봉안, 보증채무, 연대보증         ← 신설
```

**⭐ v2 에서 왜 바꿨나 (2026-08-03 실측)**

수집된 판례 112건을 주제별로 세어 보니 **불륜 0건 · 혼외자 0건**이었다.
검색은 하고 있었는데(부정행위 428건, 위자료 304건, 친생자 92건, 인지청구 70건)
한 건도 안 들어왔다. 원인 세 가지.

|원인|설명|
|---|---|
|'부정행위' 가 다른 뜻|법률 문서에서는 **세금(조세포탈)·보험(고지의무)·징계** 쪽 뜻으로 훨씬 많이 쓰인다|
|'상간자' 는 판결문에 없다|검색 결과 **단 1건**. 실제 사건명은 「손해배상(기)」 로 붙는다|
|친생자·인지청구는 가사|가정법원 사건이라 판결문이 공개되지 않는다. **282건이 '민사 아님(가사)' 로 배제**됐다|

→ 검색어를 **판결문이 실제로 쓰는 말**로 바꾸고, 혼외자는 민사 문(가액지급)으로 돌렸다.

**⚠️ D·H·I군은 아직 실적이 없는 후보다.** 수집을 한 번 돌려 실제로 걸리는지 확인한 뒤,
헛도는 검색어는 빼고 잘 걸리는 것만 남긴다. 판례 API는 무료이므로 확인 비용은 0원이다.

---

## 6. 법적 사항

### 판례 이용

| 항목 | 근거 | 결론 |
|---|---|---|
| 판결문 이용 | 저작권법 제7조 제3호 | 판결·결정·명령은 저작권 보호 대상 아님. 자유 이용 |
| 명예훼손 | 형법 제307조 | 피해자 특정 시 성립. 익명화가 유일한 방어선 |

- **가사사건(가정법원)은 판결문이 공개되지 않는다.** 열람 대상은 민사·행정·특허·형사뿐이다
- 소재는 **가족 관련 "민사" 사건**으로 한정한다
- 사용 가능: 유류분, 상속회복, 유언무효, 사인증여, 상간자 위자료, 부양료 구상금, 명의신탁, 사해행위,
  **가액지급청구(민법 제1014조)**, **제사주재자·분묘·유골**, **보증채무**
- 사용 불가: 이혼, 재산분할, 양육권, 상속재산분할, 기여분 (전부 가사)
- 가사사건을 쓰지 않으므로 **가사소송법 제10조 보도금지는 적용되지 않는다**

#### ⭐ 같은 이야기라도 "어느 문으로 들어가느냐"가 갈린다

소재를 넓힐 때 가장 중요한 원칙이다. 하고 싶은 이야기가 가사로만 가능한 것이 아니라
**대개는 민사로 들어가는 문이 따로 있다.** 그 문을 찾으면 판결문을 구할 수 있다.

|하고 싶은 이야기|막힌 문 (가사)|**열린 문 (민사)**|
|---|---|---|
|불륜|이혼 소송|**상간자 위자료 = 「손해배상(기)」**|
|혼외자|인지청구·친생자관계확인|**민법 제1014조 가액지급청구** — 상속을 다 나눈 뒤 나타난 자식이 제 몫을 돈으로 청구|
|재산 다툼|재산분할|명의신탁·사해행위취소|
|제사·유골|—|제사주재자 결정 (순수 민사, 대법원 전원합의체 있음)|

#### ⚠️ 불륜 소재를 다룰 때 (추가 규칙)

상속보다 명예훼손 위험이 크고, 자칫하면 채널 색깔이 자극물로 바뀐다.

|항목|규칙|
|---|---|
|익명화|**가상의 시·도**로 바꾸고 직업도 뭉뚱그린다. 상속편보다 한 단계 더 지운다|
|선정성|**장면 묘사 금지.** 만남·신체·숙박 묘사를 쓰지 않는다. 유튜브 수익 제한을 부른다|
|시점|"불륜 폭로"가 아니라 **"그 집이 왜 그렇게 됐나"**. 관계가 무너진 뒤의 이야기만 다룬다|
|등장인물|배신한 쪽을 악마로 그리지 않는다. 판단은 판결이 하고, 나레이션은 하지 않는다|

#### 콘텐츠 비중 (한쪽으로 몰리지 않게)

|계열|비중|
|---|---|
|상속·유류분|40% (기둥)|
|불륜·상간자|20%|
|혼외자·숨은 자식|15%|
|부양·간병·노후|15%|
|제사·분묘·보증 기타|10%|

### ⚠️ 변호사법 리스크 (반드시 준수)

**변호사법 제109조 제1호**는 변호사가 아닌 자가 금품을 받고 법률사무를 취급하는 것을 금지한다. 판례를 소재로 한 드라마 제작 자체는 법률사무 취급이 아니라고 보나, 다음은 위험하다.

| 금지 행위 | 이유 |
|---|---|
| **댓글에서 개별 사안에 답변** | 개별 법률 상담으로 평가될 수 있다 |
| "이렇게 하면 이깁니다" 식 조언 | 법률 자문 |
| 승소 가능성·전략 언급 | 법률 자문 |
| 채널을 상담처처럼 소개 | 오인 유발 |

**채널명 선정 근거**

채널명 자체가 변호사법 위반을 구성하지는 않는다. 다만 이름이 법률 상담처로 오인되면 개별 상담 댓글이 몰리고, 그것이 위험 행위를 유발하는 경로가 된다. 따라서 이름에 **픽션 신호**를 반드시 포함한다.

| 요소 | 판단 |
|---|---|
| **"극장"** | 픽션 신호. 콘텐츠 채널임이 명확해진다. **필수 유지** |
| "판결" | "판례"보다 대중적이다. 판례는 법조인 용어이고 판결은 일반인 용어다 |
| "○○의 법정" 형태 | **금지.** 「마녀의 법정」, 「아너: 그녀들의 법정」 등 방송 프로그램명이 다수 존재한다 |
| "사연" | **회피.** 이미 포화 키워드다 (사연을 부탁해, 사연튜브, 사연라디오 등). 검색에서 묻힌다 |
| "법률", "상담", "변호" | **금지.** 상담처로 오인된다 |

- 부제에 **"드라마"**를 명시한다. "이야기"보다 픽션 신호가 강하다

**필수 고지문 (3곳 배치)**

> 이 채널은 법률 상담을 제공하지 않습니다. 개별 사안은 반드시 변호사와 상담하십시오.

- 영상 설명란 / 채널 소개 / **매 회차 고정 댓글**
- 고정 댓글은 업로드 시 API로 자동 등록한다
- 확정적 법적 판단은 아니다. 채널 규모가 커지면 변호사 자문을 받는 것이 안전하다

### 익명화 (필수 치환 6항목)

| 항목 | 처리 |
|---|---|
| 성명 | 극중명으로 전면 교체 |
| **지역** | **가상의 시·도로 통째로 교체.** 실제 지명은 단 한 번도 나오면 안 된다. 해외는 국가 미특정 |
| 금액 | ±30% 이상 변형 **+ 백만원 미만 절사** (아래) |
| 연도 | 상대 표기 ("20여 년 전") |
| **판사 실명** | **완전 삭제** → "재판장". 판결문에 실명이 그대로 노출된다 |
| 사건번호 | 미표기 |

#### ⭐ 지역은 "가상의 시·도" 로 바꾼다 (강화)

예전 규칙은 "광역 단위까지 변경" 이라고만 적혀 있었다. 그러면 **진짜 도(道)를 그대로 두고
시·군만 지우는 것**으로 읽힌다. 「경남 어느 상가」 정도로 남으면 당사자를 아는 사람은 알아본다.
익명화가 명예훼손(형법 제307조)의 **유일한 방어선**인데 거기에 구멍이 생긴다.

- **실제 지명을 하나도 쓰지 않는다.** 시·도·시·군 전부.
- 가상의 지명을 지어서 쓴다. 예: `한서시` `유천시` `백운시` `서림시` `대운시`
- 굳이 넓은 지역을 말해야 하면 **「지방 소도시」 「수도권의 한 도시」** 처럼 뭉갠다.
- 실존하는 랜드마크·대학·병원 이름도 같은 이유로 쓰지 않는다.

`validate_script.py` 의 `check_region` 이 매번 자동으로 검사한다.
실제 지명이 하나라도 있으면 **영상을 만들지 않고 멈춘다.**
(오검출을 피하려고 두 갈래로 나눠 본다 — `상주(喪主)` `장수(長壽)` `예산(豫算)` `무안하다`
`영광스럽다` 같은 말은 통과시키고, `상주시` `장수군` 처럼 지명으로 쓰일 때만 잡는다.)

### ⭐ 금액은 백만원 단위로 끊는다 (필수)

끝자리가 자잘하면 귀로 들어오지 않는다. 시청자는 자릿수를 기억하지 끝자리를 기억하지 않는다.
±30% 변형 뒤 **백만원 미만 자리를 0으로 만든다.** (9억 8,412만 원 → **9억 8,400만 원**)

- 구현은 `src/money.py` 하나에 모아 두었다. 다른 곳에서 금액을 다시 계산하지 않는다.
- 적용 지점: 소재 심사(`gate.py`) → 대본 조립·보강·쇼츠(`script.py`) → 검증(`validate_script.py`)
- `amounts_used`·대사·나레이션·`amount` 그래픽에 **모두** 같은 값이 들어가야 한다.
  한 군데만 다듬으면 "본문 금액이 amounts_used 와 다르다" 검증에 걸린다.
- **예외: 100만원 미만은 그대로 둔다.** 자르면 0원이 되어 사실이 사라진다(월세 80만 원 등).
- 이미 저장된 대본을 다듬으려면: `python3 src/money.py data/scripts/EP001.json`

- 당사자는 이미 법원이 비실명 처리해 제공한다 (원고, 피고, 소외 1 등)
- 최근 3년 내 언론 대형보도 사건은 배제한다 (특정 가능성 급등)
- 영상 시작·종료·설명란 **3곳에 픽션 고지** 필수

### 유튜브 정책

- **2026년 7월 16일 시행 비진정성 콘텐츠 정책**: 일반적·반복적·템플릿 기반 콘텐츠는 수익화 배제
- AI 사용 자체는 무관. 기준은 **"인간의 독창성이 핵심인가"**
- 대본 창작 비중 82%가 이 요건 충족 근거
- **업로드 시 AI 콘텐츠 라벨 자진 표기 필수**
- 하루 다량 업로드 금지. 발행 간격 불규칙 유지

### 법령 최신성

- **유류분 제도가 2026년 3월 17일 법률 제21454호로 개정되었다**
- 2024년 4월 25일 헌법재판소 헌법불합치 결정(2020헌가4 등)에 따른 개정
- 민법 제1008조 단서 신설: 피상속인을 특별히 부양하거나 재산 유지·증가에 기여한 데 대한 보상으로 행해진 증여·유증은 그 보상 범위에서 특별수익에서 제외
- 구법은 2025년 12월 31일까지 잠정 적용되었다
- **대본의 법조문 인용은 회차마다 자동 검증한다.** 검증 근거는 **① `참조조문` 필드 → ② 비었으면
  `판례내용` 본문에서 추출한 「민법 제○○○조」 → ③ 둘 다 없으면 조문 인용 자체를 금지** 순으로 정한다.
  ⚠️ **하급심은 `참조조문` 이 항상 비어 있다**(2026-07-30 실측). 소재 대부분이 하급심이므로 ②가 기본 경로다

---

## 7. 대본 구조 (5단, 12분)

### ⭐ 이 채널은 법정물이 아니라 가족 드라마다 (절대 규칙)

시청자는 판결이 궁금해서 보지 않는다. **저 집 사람들이 왜 저렇게 됐는지**가 궁금해서 본다.
판결은 이야기의 **마침표**일 뿐 본문이 아니다.

**판결·금액에 해당하는 컷의 시간 합이 전체(720초)의 10% = 72초를 넘으면 안 된다.**
해당하는 컷: ① 4막(법정) 전체 ② `gfx.type`=`amount` 인 컷 ③ `text` 에 금액이 나오는 컷.

- 나머지 90%는 **인물 사이의 감정**이다 — 오래된 서운함, 역할의 불공평,
  말하지 못한 것, 편 가르기, 부모의 편애, 관계의 마지막 선.
- **재산은 숫자가 아니라 물건으로 말한다.** "12억 400만 원" 이 아니라 "세 아들이 다 자란 그 집".
- **도입과 1막에는 금액을 쓰지 않는다.** 시작부터 액수가 나오면 남의 재산 구경이 된다.
- `amount` 그래픽은 **회차당 1개** — 4막 판결 낭독 컷에만.
- **한 사람도 순수한 악당으로 만들지 않는다.** 양쪽 다 이해돼야 시청자가 자기 가족을 떠올린다.

검증기가 실제로 센다: `python3 src/validate_script.py data/scripts/EP001.json`
→ `[판결·금액 비중]` 항목이 초와 퍼센트를 그대로 보여준다.

### 시간 예산

| 구간 | 시간 | 길이 | 내용 |
|---|---|---|---|
| 도입 훅 | 0:00–0:22 | 22초 | 사건이 3초 안에 터진다. 설명으로 시작 금지. **금액 없음** |
| 1막 | 0:22–4:00 | 218초 | 인물 관계 확립, 주인공의 헌신·과거. **금액 없음** |
| 2막 | 4:00–7:26 | 206초 | 관계의 균열, 숨겨진 사실 발견 (반전) |
| 3막 | 7:26–10:34 | 188초 | **이야기의 절정.** 갈등 폭발, 가족이 갈라서는 순간 |
| 4막 | 10:34–11:29 | **55초** | 법정. 상대 주장 → 재판장 판단 → 금액 1번 → 듣는 얼굴 |
| 5막 | 11:29–12:00 | 31초 | 여운 + 제도 **한 문장** + 관계를 묻는 질문 |

> ⚠️ **v2.0(2026-08-01)에서 바뀌었다.** 예전에는 4막 150초·5막 50초여서
> 판결·금액이 **33.6%** 를 차지했다(EP001 실측). 4막 150→55초, 5막 50→31초로 줄이고
> 그 114초를 전부 1~3막의 사람 이야기로 옮겼다.
> **이야기의 진짜 절정은 3막 끝** — 가족이 서로 등을 돌리는 순간이다.

**판례에서 오는 것**: 사건 골격, 결말, 법리, 계산 방식
**창작해야 하는 것**: 감정선, 대사, 장면, 인물 관계 → **약 82%**

---

## 8. 영상 제작

### 방식 — 컷아웃 합성

```
블러 처리한 배경 (Gemini 생성, JPG)
  + 흰 테두리 입힌 인물 컷아웃 (Gemini 생성, 크로마키 제거, PNG)
  + 정보 그래픽 (SVG로 코드가 그림)
  + 자막
  + 나레이션 TTS + 음악 + 앰비언스 + 효과음
```

- **Google Flow는 사용하지 않는다.** 편당 수동 25분이 발생하고 자동화가 불가능하다
- **스톡 사이트(픽사베이 등)는 사용하지 않는다.** 배경도 Gemini로 생성한다
  - 근거: 한국적 배경 부재, 라이선스 제약(식별 가능한 사람·상표 금지), 파이프라인 복잡도
  - 블러가 AI 생성 결함(깨진 한글 간판, 뒤틀린 원근)을 덮는다
  - 블러 덕분에 배경 재사용이 드러나지 않는다
- **컷 길이 7초 이하.** 12분 = 약 100컷. 배경 1개당 인물 표정·크기만 바꿔 3~4컷

### 정보 그래픽 4종 — 밋밋함 해결의 핵심

기존 사연 채널이 공통으로 쓰는 장치다. SVG로 코드가 그리므로 추가 비용 0원.

| 그래픽 | 왜 필요한가 | 우선순위 |
|---|---|---|
| **타임라인** | **판결극장의 반전은 전부 시간 순서다.** "재혼 11개월 전"이 말로만 지나가면 충격이 전달되지 않는다. 연표를 화면에 그려야 한다 | **최상** |
| **가족 관계도** | 상속 사건은 관계 파악이 전제다. 도표 1장이 5분 설명을 대체한다. 없으면 시청자가 중간에 포기한다 | **최상** |
| **인물 네임태그** | 등장 시 하단에 "이정임 · 68세 · 아내". **고정 배우 7명을 회차마다 다른 역으로 쓰므로 필수다** | **최상** |
| **금액 강조** | 숫자는 귀로 들어오지 않는다. "1억 8,500만 원"은 큰 글씨로 화면에 박는다 | 상 |

### 화면 처리 규칙

| 항목 | 규칙 |
|---|---|
| **회상 구분** | 과거 장면은 저채도 + 세피아 + 비네트. 없으면 시청자가 시간을 못 따라간다 |
| **미세 움직임** | 인물 컷아웃에 **0.5% 크기 진동.** 정지 이미지의 최대 약점을 보완. 비용 0, 체감 차이 큼 |
| 카메라 무빙 | 배경에 느린 확대 또는 이동 (FFmpeg zoompan) |
| 전환 | 막 전환에 검은 화면 0.5초. 컷 전환은 하드컷 |
| 인물 배치 | 대화 장면에서 좌우 배치·크기 차이로 권력관계 표현 |
| 색보정 | 전체 톤 통일. 배경마다 색이 튀는 것 방지 |

### 자막 규칙 (고령자 타깃)

| 항목 | 규칙 |
|---|---|
| 크기 | 화면 높이의 **1/6 이상** |
| 노출 | **상시 노출.** 소리 없이도 따라올 수 있어야 한다 |
| **줄바꿈** | **어절 단위로 끊는다. 한 줄 최대 18자.** 한국어를 아무데서나 끊으면 읽기 어렵다 |
| 강조 | 핵심 단어만 색·크기 변화 |
| 위치 | 하단. 네임태그와 겹치지 않게 |

### 소리 규칙 — 아마추어 티가 가장 크게 나는 곳

| 항목 | 규칙 |
|---|---|
| **앰비언스(룸톤)** | **무음 구간을 만들지 않는다.** 완전한 무음은 "죽은 소리"로 들린다. 장소별 공기음 5종을 항상 깔아둔다 |
| **음량 정규화** | 유튜브 기준 **-14 LUFS.** FFmpeg `loudnorm` 필수. 안 맞추면 소리가 작아 이탈한다 |
| **나레이션 호흡** | 문장 사이 무음 **0.4~0.8초** 삽입. TTS는 쉼이 부족해 숨 가쁘게 들린다 |
| **목소리 분리** | 나레이터 1 + 인물별 3~4개. 나레이터가 대사까지 읽으면 인물 구분이 안 된다 |
| 재판장 대사 | 속도 10% 느리게. 무게를 준다 |

### 음악 — Lyria 3 (Gemini API)

**미리 만들어 재사용한다. 회차마다 생성하지 않는다.**

근거: 같은 프롬프트를 써도 결과가 매번 다르고, 생성 클립을 여러 번 다듬는 편집이 지원되지 않는다. 회차마다 생성하면 톤이 튄다. 시리즈물에서는 치명적이다. 고정하면 음악이 채널 정체성이 된다.

| 코드 | 용도 | 분위기 |
|---|---|---|
| `hook` | 도입 훅 | 불안, 낮은 현악 |
| `past` | 1막 회상·헌신 | 따뜻하고 애잔한 피아노 |
| `care` | 간병 장면 | 느리고 조용한, 거의 정적 |
| `reveal` | 2막 반전 발견 | 긴장 상승, 심장박동 리듬 |
| `conflict` | 3막 갈등 폭발 | 강한 타악, 불협 |
| `court` | 4막 법정 | 무겁고 절제된 긴장 |
| `verdict` | 판결 낭독 | 해소, 상승하는 현악 |
| `outro` | 5막 여운 | 잔잔한 피아노 단독 |

- Clip 모델은 30초 클립을 만든다. **FFmpeg로 이어붙이기 + 교차 페이드**로 구간 길이를 맞춘다
- 생성 오디오에도 SynthID 워터마크가 적용된다. 이미지와 같은 방침이라 문제없다
- **⚠️ 2026년 3월 초 인디 아티스트들이 학습 데이터 문제로 구글을 상대로 소송을 제기했다.** "라이선스됨"이 "다툼 없음"을 의미하지 않는다. **생성 프롬프트와 날짜를 반드시 기록**해 둔다

### 효과음 10종

문 여닫기, 발소리, 의사봉, 서류 넘기기, 전화벨, 심장박동, 시계 초침, 병원 기계음, 도장 찍기, 종이 찢기

### 이미지 생성 경로

- **반드시 API를 사용한다.** 무료 Gemini 앱은 자동화 불가 + 워터마크 부착
- API 출력에는 눈에 보이는 워터마크가 처음부터 적용되지 않는다
- SynthID(비가시)는 남으며 **남는 것이 맞다.** 유튜브 AI 라벨을 자진 표기하므로 투명성이 정책 방어 요소가 된다
- **워터마크 제거 도구는 사용하지 않는다**

---

## 9. 에셋

### 캐릭터 7명

| 코드 | 역할 | 외형 | 상태 |
|---|---|---|---|
| `F50A` | 억울한 주인공형 | 짧은 파마, 지친 눈, 남색 니트 | **생성 완료** |
| `F50B` | 악역 올케·동서형 | 단정한 단발, 날카로운 눈매 | 미생성 |
| `M50A` | 악역 장남형 | 짧은 머리 희끗, 회색 정장 | 미생성 |
| `M50B` | 차남·사위형 | 벗은 이마, 점퍼 | 미생성 |
| `F70` | 노모 | 흰 머리 파마, 굽은 어깨 | 미생성 |
| `M70` | 노부·망인 | 흰 머리, 마른 체구 | 미생성 |
| `JUDGE` | 판사 | 검은 법복 | 미생성 |

- 고정 배우진을 회차마다 돌려 쓴다. **극단 개념이며 의도된 설계다.** 어르신 시청자는 익숙한 얼굴을 선호한다
- 변호사는 목소리만 쓰고 화면은 서류 클로즈업으로 대체한다
- **약 30편 시점에 의상 변형 세트가 필요해진다.** 같은 옷이 반복되면 시청자가 알아챈다. 인물당 의상 2~3벌 추가 시트를 로드맵에 넣어둔다

### 캐릭터 시트 규격 (실측 검증 완료)

- 3열 × 6행 = 18칸, 격자선 검은색 3px
- 배경 **크로마 그린 `#00B140`**. 실측 결과 오차 5 이내 균일 → 배경 제거 정확도 확보됨
- 의상은 **남색 상의 + 검정 하의**. **회색 금지** (배경과 구분 안 됨)
- 배경 그림자 금지 (반투명 그림자는 제거 불가)
- **18번 칸(우하단)은 빈 칸으로 지시** (무료 티어 워터마크가 여기 떨어짐)
- 실측 720×1456 → 셀 240px로 부족 → **4배 업스케일 필수**

### 후처리 순서 (1회성)

```
시트 생성 → 3열 6행 슬라이싱 (격자선 4px 안쪽 트림)
  → 크로마키 배경 제거 → 알파 확장 후 흰색 채움 (인물 높이의 1.5%)
  → 4배 업스케일 → 파일명 규칙대로 커밋
```

### 파일명 규칙

```
assets/char/{인물코드}/{구도}_{표정}.png
  구도: face(클로즈업) / bust(상반신) / full(전신)
  표정: neutral / sad / anger / shock / cold / cry
  자세: stand / walk / sit / back / sit_down / door

assets/bg/{계열}_{장소}.jpg
  계열: funeral / medical / home / court / office / daily / etc

assets/bgm/{코드}.mp3
assets/amb/{장소}.mp3
assets/sfx/{이름}.mp3
```

### 배경 30장 계열

```
funeral: 접객실, 복도, 영정 앞, 주차장
medical: 1인 병실, 다인 병실, 요양원 복도, 병원 대기실
home:    거실(낮), 거실(밤), 부엌, 현관, 안방, 옷장 앞
court:   법원 외부, 법정 내부, 법원 복도
office:  변호사 사무실, 등기소, 주민센터, 은행 창구
daily:   시장 골목, 반찬가게, 식당, 카페, 공원 벤치
etc:     납골당, 시골 마당, 버스정류장, 골목길 밤
```

### 에셋 총 비용

| 에셋 | 수량 | 비용 |
|---|---|---|
| 캐릭터 시트 | 7장 (각 18컷) | 약 2천 원 |
| 배경 | 30장 | 약 8천 원 |
| 소품·손 | 3장 | 약 1천 원 |
| 음악 | 8트랙 | 소액 |
| 앰비언스 | 5종 | 소액 |
| 효과음 | 10종 | 무료 소스 또는 생성 |
| 정보 그래픽 틀 | 4종 | **0원 (코드로 생성)** |
| **합계 1회성** | | **약 1만 5천 원** |
| **회차당 추가 비용** | | **0원 (대본·TTS 호출비만)** |

---

## 10. 발행 운영

### 발행 캘린더 (`state/calendar.json`)

```
롱폼 주 4편 + 쇼츠 주 12편 = 주 16개
```

- 롱폼 발행일 기준 +1일 / +2일 / +4일에 쇼츠 1·2·3번
- **같은 날 동시 다량 업로드 금지**
- 시간대는 매번 조금씩 다르게 (완전 규칙적 발행은 양산 신호)

### 업로드 시 필수 처리

| 항목 | 내용 |
|---|---|
| AI 콘텐츠 라벨 | **자진 표기 필수** |
| 픽션 고지 | 설명란 상단 |
| **법률 상담 미제공 고지** | 설명란 + 고정 댓글 |
| 관련 법령 | 설명란에 조문 나열 |
| 챕터 마커 | 어르신은 다시 찾아보기를 쓴다 |
| 엔드 스크린 | 마지막 20초에 다음 편 + 구독 |
| 고정 댓글 | 마지막 질문을 자동 등록 |
| 재생목록 자동 추가 | 시리즈 묶기 |
| 자막 파일(.srt) | 자동 생성보다 정확. 검색 색인에 유리 |

### 검수 흐름

```
Actions가 렌더링 → 유튜브 비공개 업로드 → stage: uploaded_private
      ↓
운영자가 아이폰 유튜브 앱에서 확인
      ↓
승인 → GitHub Actions 수동 실행으로 공개 전환 → stage: published
```

### 판단 기준 (KPI) — 3개월 무수익 구간을 견디기 위한 지표

수익이 아니라 선행지표로 판단한다. 이 사업의 최대 실패 원인은 3개월 내 포기다.

| 시점 | 지표 | 기준 |
|---|---|---|
| 4주차 | 노출 클릭률(CTR) | 3% 이상 |
| **8주차** | **평균 시청 지속률** | **30% 이상 (12분 중 3.6분)** |
| 8주차 | 구독 전환 | 편당 1명 이상 |
| 12주차 | 조회수 상위 1편 | 3,000회 이상 |
| 16주차 | 수익화 요건 | 구독 1,000 + 시청 4,000시간 진입 여부 |

- **8주차 시청 지속률이 핵심 분기점**이다. 20% 미만이면 대본 구조를 재검토한다
- 지표가 미달이어도 **대본 프롬프트를 고치는 것이 우선**이다. 소재나 채널을 바꾸는 건 마지막 수단이다

---

## 11. 운영 안전장치 (필수)

| 항목 | 내용 |
|---|---|
| **실패 알림** | Actions가 실패하면 **조용히 멈춘다.** 몇 주 뒤에 알면 큰 손실이다. 실패 시 알림을 반드시 붙인다 |
| **에셋 무결성 검사** | 렌더 전에 필요한 캐릭터·배경·음악 파일이 다 있는지 확인한다. 하나 없으면 렌더가 통째로 실패한다 |
| **비용 상한** | Gemini API 종량제다. 버그로 무한 루프가 돌면 과금이 폭발한다. **일일 호출 상한 + 예산 경보**를 반드시 설정한다 |
| **법제처 호출 상한** | 하루 200회, 호출 간 1.1초 |
| **개선 루프 상한** | 대본 재작성 최대 3회 |
| **저장소 용량 감시** | 500MB 초과 시 경고. 영상이 잘못 커밋됐는지 확인 |
| **Content ID 확인** | 업로드 후 저작권 클레임 발생 여부 확인. AI 음악에서 오탐이 날 수 있다 |
| 로그 보존 | 실행 로그를 `state/`에 남긴다. 문제 추적용 |

### ⭐ 토큰을 아끼는 구조 (모델 호출 규칙)

실측: 대본 1편 = 14회 호출 · 입력 352,758 · 출력 144,368 토큰 ≈ $5.4.
**돈은 출력에서 나간다.** 입력 최적화보다 "출력을 줄이는 구조"가 훨씬 크다.

| 원칙 | 왜 | 어디에 |
|---|---|---|
| **기계적 오류는 코드로 고친다** | 정답이 하나로 정해지는 것(blackout·컷 수·초 합계·금액) 때문에 대본 전문 26,000 토큰을 주고받는 것은 순수한 낭비 | `src/autofix.py` |
| **전문을 다시 받지 않는다** | 113컷을 다시 써서 8컷을 고치면 안 된다. **바뀐 컷만** 받아 코드로 끼운다 | `script.py: apply_patch()` · `prompts/script_revise.md` |
| **반복되는 앞부분은 캐시에 올린다** | 지시문+판례 13,449 토큰이 설계 1회 + 막 6회, 총 7번 똑같이 나간다 | `claude.py: cache_prefix` |
| **채점 전에 형식을 맞춘다** | 형식 틀린 대본을 채점에 보내면 채점값이 통째로 버려진다 | `script.py: machine_fix()` |

**새 코드를 넣을 때 지킬 것**
- 모델에게 큰 덩어리를 보내기 전에 **"이걸 코드로 고칠 수 있나"** 를 먼저 묻는다.
- 모델에게 무언가를 **다시 내놓으라** 할 때는 **바뀐 부분만** 요구한다.
- 같은 글자가 두 번 이상 나가면 `cache_prefix` 로 올린다.

---

## 12. GitHub Secrets

아이폰 사파리에서 저장소 → Settings → Secrets and variables → Actions 에 등록.

```
GEMINI_API_KEY          이미지·음악·TTS 생성 (그림과 소리). 대본 대체용으로도 가능
LAW_OC                  법제처 인증키 (panryetheater) — ⚠️ 등록 불필요. 아래 설명 참조
YOUTUBE_CLIENT_ID
YOUTUBE_CLIENT_SECRET
YOUTUBE_REFRESH_TOKEN
NOTIFY_WEBHOOK          실패 알림용 (선택)
```

- ⚠️ **글은 Claude, 그림과 소리는 Gemini 로 나눈다.** 0번이 "대본 프롬프트가 유일한 핵심 자산"
  이라고 못 박듯 이 사업은 대본 품질이 전부다. 긴 한국어 감정 서사·절제된 대사는 Claude 가 낫고,
  ⭐ 2026-08-23 운영자 지시로 **Claude API 를 껐다.** 글·심사·채점·그림·목소리
  전부 `GEMINI_API_KEY` 하나로 간다 — 값이 더 싸기 때문이다.
  잠금은 두 겹이다: ① `src/claude.py` 의 writer()/grader() 가 클로드를 안 돌려준다
  ② 워크플로가 `CLAUDE_API_KEY` 를 러너에 안 넘긴다. `tools/no_claude_test.py` 가 매번 본다 —
  파이프라인이 멈추지는 않는다. 워크플로 버튼에서 어느 쪽으로 쓸지 고를 수 있다.
- ⚠️ **`LAW_OC` 는 Secrets 에 넣지 않아도 된다.** 법제처가 누구에게나 발급하는 공개 API의
  식별자이고 이 문서 5번에 평문으로 적혀 있다. 감출 것이 없는데 등록을 깜빡하면 수집이 멈춘다.
  `src/lawapi.py` 의 `DEFAULT_OC` 가 기본값을 갖고, 환경변수가 있으면 그쪽이 우선한다.
  **진짜 비밀은 `GEMINI_API_KEY` 와 유튜브 토큰 3종뿐이다. 그것들은 반드시 Secrets 에만 넣는다.**
- TTS는 **Gemini 목소리로 확정했다 (2026-08-21).** `GEMINI_API_KEY` 하나로 대본·그림·소리가
  다 돌아간다 — 관리 지점이 줄어든다는 애초 판단대로다.
  ⭐ 결정적인 까닭은 값이 아니라 **연기 지시**다. 구글 클라우드 TTS 규격에는 "어떻게 읽어라"를
  넣을 자리가 아예 없어서, 어느 목소리를 골라도 안내 방송이 됐다(운영자: "그냥 기계가 읽어주는
  놈 같다"). Gemini 는 대사와 함께 "이를 악물고 낮게 몰아붙이듯 말한다" 를 같이 받는다.
- 구글 클라우드 TTS(`GOOGLE_TTS_KEY`)는 **되돌아갈 자리**로만 남긴다. 되돌아가면 화면에 크게
  적는다 — 조용히 되돌아간 탓에 왜 어색한지 아무도 몰랐던 적이 있다.

---

## 13. 개발 로드맵

### 완료

- [x] 법제처 API 승인 및 응답 구조 실측 확인
- [x] 필터 규칙 확정 (실측 기반)
- [x] 대본 구조 확정 (5단, 12분)
- [x] 파일럿 1화 대본 작성 (창작 비중 82% 확인)
- [x] 캐릭터 시트 규격 확정 (크로마 그린 실측 검증)
- [x] `F50A` 캐릭터 시트 생성
- [x] 아키텍처 확정 (PC 불필요, GitHub Actions 단일 구조)
- [x] 2단 판정 체계 설계 (게이트 / 개선 루프)
- [x] 쇼츠 3편 전략 확정
- [x] 음악·소리·그래픽 설계

### 1순위 — 저장소 골격과 핵심 자산 ✅

- [x] 비공개 저장소 생성, 폴더 구조
- [x] **`prompts/script_gen.md` 작성** — 이 사업의 핵심 자산
- [x] `prompts/script_eval.md`, `drama_gate.md`, `shorts_gen.md`, `script_revise.md`
- [x] `state/episodes.json` 상태 관리 구조
- [x] `src/validate_script.py` — 대본 규칙을 기계가 30개 항목 검사
- [ ] Secrets 등록 (`GEMINI_API_KEY` 가 최우선) ← **운영자가 할 일**
- [ ] **반전이 약한 판례로 2편째 교차 검증** ← **최대 리스크. 여기서 갈린다**

> 1건 교차 검증 완료(608371, 대구지법 유류분). 기계 검증 30항목 통과, 채점 87점.
> 다만 그 판례는 사인증여 계약일이 재혼 11개월 전이라 반전이 이미 강했다.
> 파일럿 1화와 같은 종류의 행운이므로 **평범한 판례로 한 번 더 해봐야 한다.**

### 2순위 — 에셋

- [x] `build-assets.yml` — 생성부터 커밋까지 전자동
- [x] 시트 후처리 (슬라이싱 → 크로마키 → 흰 테두리 → 4배 업스케일)
- [x] **앰비언스 5종 + 효과음 10종 합성 완료 (비용 0원)**
- [x] 정보 그래픽 4종 — 코드가 직접 그린다
- [ ] 캐릭터 6명 추가 생성 (F50A 시트만 있음)
- [ ] 배경 30장 생성
- [ ] 음악 8트랙 — 합성으로는 불가. 미리 만들어 넣어야 한다

### 3순위 — 수집 ✅

- [x] `collect.yml` — 판례 수집 + 3단 게이트
- [x] 1차 하드 배제 · 2차 기계 가점 · 3차 LLM 드라마성 평가
- [ ] 백필 실행 (하루 200회 상한, 3일 분산) ← 버튼만 누르면 됨

### 4순위 — 대본 ✅

- [x] `script.yml` — 생성 + 평가 + 부분 개선 (최대 3회)
- [x] 익명화 6항목 자동 치환 + 기계 검사
- [x] 법조문 자동 검증 (3단 폴백: 참조조문 → 본문추출 → 인용금지)
- [x] 쇼츠 3편 구간 지정 및 대본
- [x] 소재 유형 연속 2편 초과 금지

### 5순위 — 렌더링·발행 ✅

- [x] `produce.yml` — 블러·합성·무빙·그래픽·자막·소리·인코딩
- [x] 16:9 롱폼 + 9:16 쇼츠 3편 동시 출력
- [x] TTS 연동 (인물별 목소리 분리, 재판장 10% 느리게)
- [x] 유튜브 업로드 + 고지문 + 자막(.srt) + 챕터 + 재생목록 + 댓글
- [x] `publish.yml` — 검수 후 공개 전환, 쇼츠 +1/+2/+4일 분산
- [ ] AI 라벨·댓글 고정은 API 미지원 → 스튜디오에서 수동

### 6순위 — 학습 루프 ✅

- [x] `stats.yml` — 조회수·유지율·CTR 수집
- [x] `rejected.json` 을 유형별로 집계해 게이트 보정 근거 산출
- [ ] 실제 데이터가 쌓인 뒤 게이트 기준 조정

### 7순위 — 관리자 페이지 ✅

- [x] Cloudflare Workers 한 파일. 아이폰 화면 검증 완료
- [x] 대본 읽기 — 먼저 볼 세 곳 + 5단 전문
- [x] 워크플로 6개 버튼 실행
- [x] `deploy-admin.yml` 자동 배포

### 나중에

- [ ] 의상 변형 세트 (약 30편 시점)
- [ ] 썸네일 A/B 테스트
- [ ] 일본 시장 확장 (한국에서 공식 검증 후)

---

## 14. 미확정 항목

| 항목 | 상태 |
|---|---|
| **대본 생성 프롬프트** | **미작성. 최우선 과제** |
| **대본 재현성** | **n=1. 2편째 검증 필요 (최대 리스크)** |
| ~~TTS 서비스~~ | **확정 (2026-08-21): Gemini 목소리.** 연기 지시를 함께 보낼 수 있는 것이 결정적이었다 |
| 법제처 API 일일 호출 한도 | 미공개. 하루 200회로 대응 |
| 이미지 생성 모델 최대 해상도 | 확인 필요. 부족하면 4배 업스케일 |
| 하급심 판례 API 수록 비율 | **재측정됨 (2026-07-30): 유류분 89건 중 하급심 민사 판결 24건.** 종전 추정치(약 7건)의 3배 이상. 다만 검색어 1개 기준이므로 40개 전체 비율은 백필 때 확인 |
| Lyria 저작권 분쟁 경과 | 소송 진행 중. 프롬프트·날짜 기록으로 대비 |
| Claude Code 모바일 앱 세부 동작 | 실제 사용으로 확인 |

---

## 15. 소통 규칙

- 존댓말 사용
- 비판적·객관적으로 평가. 목표 부합성을 기준으로 판단
- 요점만. 돌려 말하지 않는다
- 코딩 전문용어는 **첫 사용 시 풀네임 + 아주 간략한 설명** 필수
- 답변 마지막에 요약 결론
- ⭐ **뭘 하라는지는 반드시 답변 맨 끝에 둔다** (2026-08-14 손님: "뭘 들으라는
  거야? 결론을 끝에 얘기를 해줘야지 항상 뭘 하라는지.")
  설명·근거·표를 먼저 쓰고, **맨 마지막 줄에 '지금 하실 것' 한 가지**를 적는다.
  누를 버튼 이름을 그대로 적는다. 중간에 끼워 넣으면 못 찾는다.
- 확실하지 않은 것은 "확실하지 않음"이라고 명시
- 추측한 경우 "추측입니다"라고 표시
- 관련 법령·규정 근거를 명시
- **아이폰 환경을 전제로 제안한다.** PC나 터미널이 필요한 방법은 제안하지 않는다
