# 판결극장 — 클로드 코드 착수 안내 (아이폰 전용)

**소요 시간** 1단계~4단계 약 40분 / 5단계부터 개발

---

## 0단계 — 기존 채널을 쓸지 결정 (판단 필요)

기존에 「미래에서 온 만물상」 채널이 있습니다. 이걸 전환할지 새로 만들지는 **수익화 요건 충족 여부**로 갈립니다.

| 기존 채널 상태 | 결정 |
|---|---|
| **구독 1,000명 + 시청 4,000시간 달성** | **기존 채널 전환.** 압도적으로 유리합니다. 이 요건을 다시 채우는 데 최소 6개월이 걸립니다 |
| 요건 미달, 구독 수백 명 | **새 채널 권장.** 주제가 완전히 다르면 기존 시청 이력이 알고리즘을 혼란시킵니다 |
| 요건 미달, 구독 수십 명 | 새 채널. 잃을 것이 없습니다 |

**기존 채널을 전환하는 경우 순서**

1. **기존 영상 전부 비공개 처리** ← 가장 중요합니다. 남겨두면 알고리즘이 엉뚱한 시청자에게 추천합니다
2. 채널명 변경 → 판결극장
3. 핸들 변경 → 예: `@verdicttheater`
4. 프로필 사진, 채널 배너 교체
5. 채널 설명 교체 (아래 문구 사용)
6. 카테고리 변경 → 엔터테인먼트 또는 영화·애니메이션
7. 재생목록 전부 삭제 또는 비공개
8. 기존 구독자 이탈은 감수합니다. 주제가 다르므로 자연스러운 일입니다

**⚠️ 유튜브는 채널명 변경 횟수를 제한합니다.** 14일 내 3회까지입니다. **한 번에 확정해서 바꾸십시오.**

**채널 설명 문구 (그대로 사용 가능)**

```
실제 법원 판결을 바탕으로 재구성한 가족 이야기입니다.

상속, 유류분, 부양, 재산 다툼.
법정까지 간 가족들의 사연을 드라마로 만듭니다.

※ 등장인물의 이름, 지역, 직업, 금액은 모두 각색되었으며
   실제 사건 당사자와 무관합니다.
※ 이 채널은 법률 상담을 제공하지 않습니다.
   개별 사안은 반드시 변호사와 상담하십시오.
※ 이 채널의 영상은 AI 기술을 활용해 제작됩니다.
```

- 마지막 세 줄이 **픽션 고지 · 변호사법 방어 · AI 라벨 방어**를 동시에 처리합니다

---

## 1단계 — GitHub 계정과 저장소 만들기 (10분)

**사파리에서 github.com 접속**

1. 계정이 없으면 가입 (무료)
2. 오른쪽 위 `+` → **New repository**
3. 설정
   - Repository name: `verdict-theater`
   - **Private 선택** ← 반드시. 프롬프트가 핵심 자산이라 공개하면 안 됩니다
   - Add a README file 체크
4. **Create repository**

**⚠️ Private을 꼭 선택하십시오.** 공개 저장소는 무료 Actions 시간이 무제한이지만, 대본 생성 프롬프트가 그대로 노출됩니다. 비공개 월 2,000분으로도 필요량의 11%만 씁니다.

---

## 2단계 — 지침서 올리기 (5분)

1. 저장소 화면에서 **Add file** → **Create new file**
2. 파일명 칸에 정확히 **`CLAUDE.md`** 입력
3. 제가 드린 `CLAUDE.md` 내용을 **전체 복사해서 붙여넣기**
4. 아래 **Commit changes**

- 이 파일이 있으면 클로드 코드가 시작할 때 자동으로 읽습니다. **지금까지 정한 결정을 다시 설명할 필요가 없어집니다**

---

## 3단계 — 열쇠(API 키) 준비 (15분)

### 3-1. Gemini API 키

1. 사파리에서 **aistudio.google.com** 접속
2. 구글 계정으로 로그인
3. **Get API key** → **Create API key**
4. 생성된 문자열을 복사해서 메모 앱에 임시 보관

**⚠️ 유료 결제 설정이 필요합니다.** 이미지·음악 생성은 무료 등급에서 제한됩니다. 그리고 무료 등급은 눈에 보이는 워터마크가 붙습니다.

### 3-2. 유튜브 업로드 열쇠

이게 가장 번거로운 단계입니다. 아이폰에서도 됩니다.

1. **console.cloud.google.com** 접속
2. 새 프로젝트 만들기 (이름: `panrye`)
3. **API 및 서비스** → **라이브러리** → `YouTube Data API v3` 검색 → **사용**
4. **API 및 서비스** → **OAuth 동의 화면** → 외부 → 앱 이름·이메일 입력 → 저장
5. **사용자 인증 정보** → **사용자 인증 정보 만들기** → **OAuth 클라이언트 ID**
   - 애플리케이션 유형: **웹 애플리케이션**
   - 승인된 리디렉션 URI에 추가: `https://developers.google.com/oauthplayground`
6. 나온 **클라이언트 ID**와 **클라이언트 보안 비밀**을 메모

**갱신 토큰(refresh token) 받기**

1. **developers.google.com/oauthplayground** 접속
2. 오른쪽 위 톱니바퀴 → **Use your own OAuth credentials** 체크
3. 위에서 받은 클라이언트 ID와 보안 비밀 입력
4. 왼쪽 목록에서 `YouTube Data API v3` → `https://www.googleapis.com/auth/youtube` 선택
5. **Authorize APIs** → 구글 로그인 → 허용
6. **Exchange authorization code for tokens** 누르기
7. 나온 **Refresh token** 복사

**막히면 이 단계는 나중으로 미루십시오.** 업로드는 5순위 작업이라 지금 당장 필요하지 않습니다.

---

## 4단계 — Secrets 등록 (5분)

1. 저장소 → **Settings** (아래로 스크롤하면 있습니다)
2. 왼쪽 메뉴 → **Secrets and variables** → **Actions**
3. **New repository secret** 눌러 하나씩 추가

| Name | Secret |
|---|---|
| `LAW_OC` | `panryetheater` |
| `GEMINI_API_KEY` | 3-1에서 받은 키 |
| `YOUTUBE_CLIENT_ID` | 3-2의 클라이언트 ID |
| `YOUTUBE_CLIENT_SECRET` | 3-2의 보안 비밀 |
| `YOUTUBE_REFRESH_TOKEN` | 3-2의 Refresh token |

- 한 번 저장하면 다시 볼 수 없습니다. **메모에 원본을 남겨두십시오**
- Secrets에 넣은 값은 Actions 로그에도 가려져서 나옵니다. 안전합니다

---

## 5단계 — 클로드 코드에서 저장소 연결

1. 클로드 앱에서 **클로드 코드**로 들어갑니다
2. GitHub 계정 연결
3. `verdict-theater` 저장소 선택

**연결되면 아래 지시문을 그대로 붙여넣으십시오.**

---

## 첫 지시문 (복사해서 붙여넣기)

```
CLAUDE.md를 먼저 읽어주십시오. 이 프로젝트의 모든 결정사항이 담겨 있습니다.

읽으신 뒤, 1순위 작업부터 진행하겠습니다.

[중요한 전제]
저는 코딩을 모릅니다. 아이폰만 씁니다. PC와 터미널이 없습니다.
따라서 제가 직접 명령어를 실행해야 하는 방법은 제안하지 마십시오.
모든 실행은 GitHub Actions 안에서 일어나야 하고,
저는 GitHub 앱에서 버튼을 누르는 것만 해야 합니다.

[첫 작업 — 순서대로]

1. 저장소 폴더 구조를 만들어 주십시오.
   CLAUDE.md의 "2. 시스템 구성"에 있는 구조 그대로입니다.
   빈 폴더는 .gitkeep 파일로 만들어 주십시오.

2. state/episodes.json 초기 파일을 만들어 주십시오.
   CLAUDE.md "3. 회차 상태 관리" 형식을 따릅니다.

3. prompts/script_gen.md 를 작성해 주십시오.
   이게 이 프로젝트에서 가장 중요한 파일입니다.

   조건:
   - 입력은 판례 원문 JSON (판시사항, 판결요지, 참조조문, 판례내용)
   - 출력은 12분 5단 구조 대본 + 장면별 에셋 지정 + 쇼츠 3편 구간 지정
   - 익명화 6항목을 반드시 치환
   - 판사 실명은 완전 삭제
   - CLAUDE.md의 "7. 대본 구조"와 "4-3. 쇼츠"를 따를 것
   - 창작 비중 82%를 감당해야 함. 감정선·대사·장면을 모두 만들어야 함
   - 3초 안에 갈등이 터져야 하고, 설명으로 시작하면 안 됨

   작성 전에 저에게 확인이 필요한 사항이 있으면 먼저 질문해 주십시오.

4. prompts/script_eval.md 를 작성해 주십시오.
   CLAUDE.md "4-2. 개선 루프"의 5항목 100점 루브릭을 따릅니다.
   자기가 쓴 대본이라는 정보를 주지 않는 별도 평가용 프롬프트입니다.

한 번에 다 하지 마시고, 1번과 2번 먼저 하고 저에게 보여주십시오.
```

---

## 개발 순서 — 이 순서를 지키십시오

| 순서 | 작업 | 왜 이 순서인가 |
|---|---|---|
| **1** | 폴더 구조 + 상태 파일 | 그릇을 먼저 만든다 |
| **2** | **대본 생성 프롬프트** | **핵심 자산. 이게 없으면 나머지가 무의미** |
| **3** | **2편째 교차 검증** | **재현되는지 확인. 여기서 사업 성립이 갈린다** |
| 4 | 에셋 생성 워크플로 | 검증 통과 후 |
| 5 | 판례 수집 워크플로 + 백필 | |
| 6 | 대본 워크플로 (생성+평가+개선) | |
| 7 | 렌더링 + 업로드 워크플로 | 가장 복잡. 마지막 |
| 8 | 성과 수집 + 학습 루프 | 발행 시작 후 |

**3번을 건너뛰지 마십시오.**

파일럿 1화는 원본 판례에 "재혼 11개월 전 계약"이라는 강한 반전이 이미 있었습니다. 운이 좋았습니다. 반전이 없는 평범한 판례에서 대본이 무너지면, 이건 파이프라인이 아니라 수공업입니다. 이 검증 없이 4번 이후로 진행하면 나중에 전부 다시 써야 합니다.

---

## 검증 체크포인트

각 단계를 넘어가기 전에 확인하십시오.

| 단계 | 통과 조건 |
|---|---|
| 프롬프트 완성 | 파일럿 1화와 **같은 수준**의 대본이 나오는가 |
| **2편째 검증** | **다른 판례에서도 80점 이상이 나오는가** |
| 에셋 생성 | 캐릭터 얼굴이 시트 간에 일관되고, 배경 제거가 깨끗한가 |
| 수집 | 세금 사건이 걸러지고, 하급심이 잡히는가 |
| 렌더링 | 12분 영상에 그래픽 4종·자막·소리가 다 들어갔는가 |
| 업로드 | AI 라벨·픽션 고지·법률 상담 미제공 고지가 다 붙었는가 |

---

## 이것만은 기억하십시오

| 원칙 | 내용 |
|---|---|
| **PC가 필요한 방법은 거절하십시오** | 클로드 코드가 터미널 명령을 제안하면 "아이폰만 있다"고 다시 말하십시오 |
| **영상을 GitHub에 넣지 마십시오** | 지워도 이력에 남아 되돌릴 수 없습니다 |
| **비용 상한을 먼저 설정하십시오** | 버그로 무한 루프가 돌면 과금이 폭발합니다 |
| **실패 알림을 반드시 붙이십시오** | Actions는 실패하면 조용히 멈춥니다 |
| **법률 상담에 답하지 마십시오** | 변호사법 리스크입니다. 고정 댓글로 명시하십시오 |
| **한 번에 다 하려 하지 마십시오** | 단계마다 확인하고 넘어가야 나중에 다시 안 씁니다 |

---

## 막혔을 때

| 상황 | 대응 |
|---|---|
| 클로드 코드가 터미널 명령을 요구 | "아이폰만 있습니다. Actions로 해주십시오" |
| Actions가 실패 | GitHub 앱 → Actions → 실패한 실행 → 로그 확인 후 그 내용을 클로드 코드에 붙여넣기 |
| 유튜브 열쇠 발급 실패 | 5순위 작업이니 나중으로 미루고 1~4순위 먼저 |
| 저장소가 500MB 초과 | 영상이 잘못 커밋된 것. 즉시 확인 |
| 대본 품질이 안 나옴 | 프롬프트를 고치십시오. 소재나 채널을 바꾸는 건 마지막 수단입니다 |
